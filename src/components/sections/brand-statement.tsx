/**
 * ۰۱ — جمله‌ی برند. بلافاصله بعد از هیرو.
 *
 * ── یک پاراگراف، شش خط، یک‌درمیان قرمز ──
 * پیش‌تر سه بلوکِ جدا بود با سه اندازه‌ی متفاوت و یک خطِ رُز کنارِ سومی.
 * بابک گفت هر سه باید «در قالب یک پاراگراف» باشند و «چینشِ جمله‌ها همان
 * شکلی که تایپ کردم»، و «جمله‌ی دوم و پایینیِ هر خط قرمز».
 *
 * پس: یک بلوک، یک اندازه، شش خط. هر جفت سرِ نقطه‌ویرگول می‌شکند و خطِ
 * دومش رُز است. شکست **دستی** است و نه تصادفی: با شکستِ خودکار «مسیر»
 * ته خط از «من» جدا می‌افتاد و «چیزی» از «است» — در فارسی این‌ها تکه‌های
 * یک عبارت‌اند و جداشدنشان خواندن را می‌لنگاند.
 *
 * اندازه‌ی فونت از روی **بلندترین خط** حساب شد، نه سلیقه. آن خط
 * («توسعه‌ی فردی، … سازمان؛») باید در دسکتاپ روی یک خط بماند؛ اگر
 * بشکند، چینشی که بابک خواست به هم می‌خورد. عرضِ محتوا در دسکتاپِ کامل
 * ۱۰۳۲ پیکسل است و همان جمله اندازه گرفته شد: در ۳۲px عرضش ۱۰۱۶ و در
 * ۳۴px می‌شود ۱۰۷۹. پس سقف روی **۳۱** نشست تا حاشیه‌ی امن بماند.
 * `max-w` هم برداشته شد چون همان بود که خط را می‌شکست.
 *
 * زمینه روشن است و نه تیره: هیرو تیره است و بخشِ داستان هم؛ این بخش
 * نفسِ روشنِ میانِ آن دو است.
 *
 * بالای بخش یک نوارِ گرادیانی هست که درزِ هیرو را می‌بندد. توضیحِ کاملش
 * کنارِ خودش و در `hero-v3-video.tsx` است.
 */
export default function BrandStatement() {
  return (
    <section
      aria-labelledby="statement-title"
      className="relative isolate overflow-hidden bg-bone pb-[clamp(84px,15vh,190px)] pt-[clamp(130px,26vh,320px)] text-[#1f1a16]"
      id="statement"
    >
      {/* نیمه‌ی دومِ درزِ هیرو. از همان #4d1214 شروع می‌شود که کفِ هیرو در
          آن حل شده، پس هیچ خطی بینشان نیست.
          نسخه‌ی اول ۹۹ پیکسل بود و با اینکه ریاضیاً هیچ درزی نداشت، چشم
          یک «نوار» می‌دید: شیب آن‌قدر کوتاه بود که گذار ناگهانی می‌نمود.
          حالا ۱۹vh است و `padding-top` بخش هم با آن بالا رفت.

          ارتفاعش عمداً از padding بالای بخش کوچک‌تر مانده (۱۹vh در برابر
          ۲۶vh، و ۹۶ در برابر ۱۳۰، و ۲۳۰ در برابر ۳۲۰): وگرنه تیتر روی
          قسمتِ تیره‌ی گرادیان می‌افتاد و تضادش می‌شکست. */}
      <div
        aria-hidden="true"
        className="pointer-events-none absolute inset-x-0 top-0 h-[clamp(96px,19vh,230px)] bg-gradient-to-b from-[#4d1214] to-bone"
      />

      <div className="mx-auto max-w-[1160px] px-[clamp(20px,5vw,64px)]">
        <p
          className="reveal font-display text-[clamp(18px,2.3vw,31px)] font-extrabold leading-[1.75] tracking-tight"
          id="statement-title"
        >
          <span className="block">
            توسعه‌ی فردی، ورود به کسب‌وکار، دوازده سال بازاریابی شبکه‌ای و رهبری یک
            سازمان؛
          </span>
          <span className="block text-rose-deep">
            مسیر من همیشه درباره‌ی یک چیز بوده است: رشد.
          </span>

          <span className="mt-[0.9em] block">من مسیر رشد را فقط آموزش نمی‌دهم؛</span>
          <span className="block text-rose-deep">خودم آن را زندگی کرده‌ام.</span>

          <span className="mt-[0.9em] block">
            در واقع موفقیتِ من فقط چیزی نیست که به دست آورده‌ام؛
          </span>
          <span className="block text-rose-deep">
            چیزی است که در دیگران ایجاد کرده‌ام.
          </span>
        </p>
      </div>
    </section>
  );
}
