"use client";

import { useRef } from "react";
import { useVideoInView } from "@/lib/use-video-in-view";
import { asset } from "@/lib/asset";

/**
 * هیرو — ویدیوی سارا، تمام‌صفحه، با متن در سمت راست.
 *
 * کلیپِ خودِ بابک، تدوین‌شده بر اساس هیروی مرجع (ellieswift.com):
 *   • کادر: پنجره‌ی ۱۶:۹ به عرض ۱۲۰۲ با لبه‌ی راست روی لبه‌ی راستِ منبع،
 *     از بالا لنگرانداخته (خروجی ۱۲۰۲×۶۷۶). لبه‌ی راست ثابت می‌مانَد تا
 *     ورودِ از راست حفظ شود؛ عرضِ کم‌تر همزمان سه چیز می‌دهد: زوم بیشتر،
 *     نشستنِ سارا در نیمه‌ی چپ (۴۵٪ عرض)، و خروجِ کاملش از کادر.
 *   • ورود با تاری نسبی که تا ثانیه‌ی ۱.۹ به صفر می‌رسد.
 *   • مکث و لبخند با سرعت ۰.۶۲× (اسلوموشن).
 *   • خروج کامل با سرعتِ عادی؛ در ثانیه‌ی ۷.۶ از کادر بیرون می‌رود و
 *     ۰.۳s کادرِ خالی می‌مانَد که درزِ لوپ را می‌بندد.
 * جزئیات و اعداد در HANDOFF.md.
 *
 * صدا حذف شده — سلیقه‌ای نیست: autoplay در همه‌ی مرورگرها فقط برای
 * ویدیوی بی‌صدا مجاز است.
 *
 * ── زمینه‌ی قرمز ──
 * ویدیو را **خودِ بابک** با زمینه‌ی قرمز رندر کرد، پس هیچ ماتی در کار
 * نیست. قرمزِ او اندازه‌گیری شد: میانه‌ی **#66191b** — روشن‌تر و گرم‌تر
 * از قرمزِ پرتره‌ی استودیویی (#53040e). پس رنگِ بخش و پرده با همان
 * هماهنگ شد، نه با توکن‌های `crimson` که ته‌رنگشان فرق دارد.
 *
 * نسخه‌ی کرِم (`sara-hero.*`) دست‌نخورده مانده؛ برگرداندن یعنی سه مسیر
 * در همین فایل، رنگ‌های متن و پرده، و `navTone` در رجیستری.
 *
 * با عوض‌شدنِ زمینه، متن هم باید عوض می‌شد: روی کرِم مرکبِ تیره بود، حالا
 * روی قرمزِ تیره روشن است. `navTone` در رجیستری هم به `onDark` رفت.
 *
 * ── متن ──
 * سارا در طول مکث x را از ۲۸٪ تا ۷۲٪ کادر می‌گیرد، پس نوار ۷۲–۱۰۰٪ خالی
 * است و ستون متن همان‌جا می‌نشیند.
 *
 * پرده‌ی کنارِ متن عمدی است و تزئینی نیست، و عمقش **اندازه‌گیری‌شده**:
 * سارا هنگام ورود و خروج از همان نوار عبور می‌کند و روشناییِ پشتِ متن به
 * ۰.۳۲ می‌رسد. با پرده‌ی ۰.۶ خطِ نقش فقط ۳.۶۹:۱ می‌شد. حالا پرده ۰.۷۸
 * است و متن هم روشن‌تر شد.
 *
 * قرمزِ بابک (#66191b) از قرمزِ پرتره‌ی استودیویی (#53040e) روشن‌تر است،
 * پس حاشیه‌ی کنتراست کم‌تر است و این عددها با آن تنظیم شده‌اند.
 */
export default function HeroVideo() {
  const wrapRef = useRef<HTMLDivElement>(null);
  const videoRef = useRef<HTMLVideoElement>(null);
  useVideoInView(wrapRef, videoRef);

  return (
    <section
      ref={wrapRef}
      aria-labelledby="hero-name"
      className="relative h-svh w-full overflow-hidden bg-[#4d1214]"
    >
      <video
        ref={videoRef}
        autoPlay
        /* در موبایل کادر عمودی می‌شود و object-cover فقط ۲۶٪ عرضِ ویدیو را
           نشان می‌دهد. سارا از راست وارد و از چپ خارج می‌شود و با برشِ
           مرکزی فقط ۶۷٪ زمان در کادر می‌ماند. نقطه‌ی برش روی ۴۰٪ (چون سارا
           در نیمه‌ی چپ کادر است و میانه‌ی موقعیتش ۴۵٪ است) آن را به ۷۵٪
           می‌رساند. در دسکتاپ برش تقریباً صفر است. */
        className="h-full w-full object-cover object-[40%_center] lg:object-center"
        loop
        muted
        playsInline
        poster={asset("/video/sara-hero-red-poster.jpg")}
        preload="metadata"
        tabIndex={-1}
      >
        <source src={asset("/video/sara-hero-red.webm")} type="video/webm" />
        <source src={asset("/video/sara-hero-red.mp4")} type="video/mp4" />
      </video>

      {/* پرده — از لبه‌ی راست، هم‌رنگِ دیوار. در دسکتاپ عمودی نیست بلکه
          افقی است تا فقط ستون متن را بگیرد؛ در موبایل از پایین می‌آید،
          چون متن آن‌جا پایین می‌نشیند. */}
      <div
        aria-hidden="true"
        className="pointer-events-none absolute inset-0
                   bg-[linear-gradient(to_top,rgba(38,9,10,0.96)_0%,rgba(38,9,10,0.82)_28%,transparent_62%)]
                   lg:bg-[linear-gradient(to_left,rgba(38,9,10,0.95)_0%,rgba(38,9,10,0.78)_18%,transparent_44%)]"
      />

      {/* درزِ هیرو و بخشِ بعدی.
          بابک گفت مرزِ این دو «خشک» است و خطِ صافش دیده می‌شود. دو تکه
          دارد و هر دو لازم‌اند:
            ۱. همین لایه، که کفِ ویدیو را در رنگِ خودِ بخش (#4d1214) حل
               می‌کند، پس لبه‌ی پایینیِ هیرو دیگر یک فریمِ ویدیو نیست بلکه
               یک رنگِ تخت است.
            ۲. نوارِ گرادیانی در بالای `brand-statement.tsx` که از همان
               رنگ شروع می‌شود و به کرِم می‌رسد.
          با هم، هیچ لبه‌ای نمی‌مانَد.

          **بعد از پرده** می‌آید و نه قبلش، عمداً: در موبایل پرده تا نزدیکِ
          کدر تیره می‌شود و اگر این زیرش بود دیده نمی‌شد. روی پرده،
          کفِ کادر #4d1214 می‌شود و متنِ `text-paper` روی آن ۱۲.۴:۱
          می‌گیرد — بهتر از روی خودِ ویدیو. */}
      <div
        aria-hidden="true"
        className="pointer-events-none absolute inset-x-0 bottom-0 h-[16%] bg-gradient-to-b from-transparent to-[#4d1214] lg:h-[20%]"
      />

      {/* ستون متن — سمت راستِ فیزیکی.
          دو نکته‌ی RTL که هر دو یک‌بار اشتباه شد:
          • `right-0` و نه `end-0`: محور منطقی برعکس می‌شود و ستون به چپ می‌افتد.
          • `items-start` و نه `items-end`: در RTL محورِ عرضیِ فلکس از راست
            شروع می‌شود، پس «start» راستِ فیزیکی است. با `items-end` تیتر
            به چپِ جعبه چسبید و از padding بیرون زد.
          در دسکتاپ فقط از راست padding می‌گیرد، نه دو طرف: ستون به لبه‌ی
          راست چسبیده و padding چپ فقط عرضِ مفید را می‌خورد و تیتر را
          سرریز می‌کند.

          حاشیه‌ی راست با عرضِ پنجره رشد می‌کند (`6.5vw`) و ثابت نیست: نوارِ
          خالیِ سمت راست با پهن‌شدنِ پنجره بزرگ‌تر می‌شود، و با حاشیه‌ی ثابت
          متن به لبه می‌چسبد و یک فاصله‌ی هدررفته بین سارا و متن می‌مانَد.
          با این نسبت، بلوک تقریباً وسطِ همان نوار می‌نشیند و در هیچ عرضی
          با سارا (لبه‌ی راستش ۷۲٪) برخورد نمی‌کند.

          نرخِ رشدِ اندازه‌ی نام (`5.2vw`) هم به همین قید بسته است، نه به
          سلیقه: نوارِ خالی ۲۸٪ عرض است و نام + حاشیه باید در آن جا شود.
          با ۵.۶vw نام در عرض ۱۴۴۰ تا ۲۱px به شال سارا می‌رسید. */}
      <div
        className="absolute bottom-0 right-0 left-0 flex flex-col items-start
                   px-[clamp(20px,5vw,56px)] pb-[clamp(28px,6vh,64px)] text-right
                   lg:bottom-auto lg:left-auto lg:top-1/2 lg:-translate-y-1/2
                   lg:w-[clamp(300px,32vw,520px)] lg:px-0 lg:pb-0
                   lg:pr-[clamp(48px,6.5vw,150px)]"
      >
        <h1
          id="hero-name"
          className="font-display font-extrabold leading-[1.04] tracking-tight text-paper
                     text-[clamp(40px,9vw,88px)] lg:text-[clamp(44px,5.2vw,88px)]"
        >
          <span className="block">سارا</span>
          <span className="block">داودی‌فر</span>
        </h1>

        <p className="mt-[clamp(14px,2.4vh,26px)] text-[clamp(11px,2.6vw,14px)] tracking-[0.14em] text-paper/85 lg:text-[clamp(11px,1.05vw,13.5px)]">
          کارآفرین · رهبر · مربی · ورزشکار
        </p>

        <p className="mt-[clamp(18px,3.2vh,34px)] text-[clamp(13px,3.4vw,17px)] leading-[2.1] text-paper lg:text-[clamp(13px,1.25vw,17px)]">
          <span className="block">انضباط را از ورزش آوردم</span>
          <span className="block">به کسب‌وکار سپردم</span>
          <span className="block">و حالا به آدم‌ها می‌سپارم</span>
        </p>
      </div>
    </section>
  );
}
