import Image from "next/image";
import { asset } from "@/lib/asset";

/**
 * هیرو نسخه‌ی ۲ — تصویر «ABOUT ME».
 *
 * یک گرافیکِ آماده و کامل است که بابک ساخته: تیتر، امضا، پاراگراف و
 * فوترش همه داخل خودِ تصویر است. پس هیرو **هیچ لایه‌ی متنی ندارد** —
 * هرچه بگذاریم روی متنِ خودِ تصویر می‌افتد.
 *
 * نسخه‌ی قبلی (کینتیک ادیتوریال با کارت سه‌بعدی و اسمِ گرادیانی) کامل
 * برداشته شد؛ خواسته این بود که کلِ هیرو همین تصویر باشد.
 *
 * نسبت تصویر ۱.۷۹۲ است و نسبت هیرو در دسکتاپ حدود ۱.۷۶، پس `object-cover`
 * برشِ ناچیزی می‌زند. در موبایل که کادر عمودی می‌شود برش سنگین است و متنِ
 * داخل تصویر بریده می‌شود — به همین دلیل آن‌جا `object-contain` است و
 * تصویر کامل، وسطِ کادر، روی زمینه‌ی خودش می‌نشیند.
 *
 * سرور-کامپوننت است: نه تعاملی دارد و نه استیتی، پس `use client` لازم
 * ندارد و از باندل کلاینت بیرون می‌مانَد.
 */
export default function HeroAbout() {
  return (
    <section
      aria-label="درباره‌ی سارا داودی‌فر"
      className="relative h-svh w-full overflow-hidden bg-[#2b2b2b]"
    >
      <Image
        alt="درباره‌ی سارا داودی‌فر — کارآفرین، رهبر و مربی با دوازده سال تجربه در نتورک مارکتینگ"
        className="object-contain lg:object-cover"
        fill
        preload
        quality={88}
        sizes="100vw"
        src={asset("/images/hero2-about-2560.webp")}
      />
    </section>
  );
}
