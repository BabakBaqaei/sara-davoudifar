import Image from "next/image";
import SectionShell from "@/components/site/section-shell";
import { asset } from "@/lib/asset";

/**
 * درباره‌ی سارا — معرفیِ کوتاه، بلافاصله بعد از جمله‌ی برند.
 *
 * ── چرا جابه‌جا شد ──
 * این بخش تا امروز آخرِ سایت بود، با این استدلال که «About» در بالا سایت
 * را به رزومه تبدیل می‌کند. بابک خواست بیاید بالا، بین جمله‌ی برند و
 * داستان. حق با اوست و استدلالِ قبلی فقط نصفش درست بود: چیزی که سایت را
 * رزومه می‌کند، *جای* معرفی نیست، *جنسِ* آن است. پس معرفی آمد بالا ولی
 * جنسش عوض شد — نه فهرستِ دستاورد، بلکه «من کی‌ام و با چه کسانی کار
 * می‌کنم»، که مقدمه‌ی طبیعیِ داستانِ بعدی است.
 *
 * **دو تا نشد، یکی ماند.** بخشِ پایینیِ «درباره» حذف نشد بلکه همین است که
 * بالا آمد. دو معرفیِ پرتره‌دار در یک صفحه، همان تکرارِ مکرراتی می‌شد که
 * بابک قبلاً از آن گله داشت.
 *
 * ── متن ──
 * متنِ نهایی را خودِ بابک داد و عیناً همان نشست — نه ترجمه‌ی من از کارتِ
 * «ABOUT ME» که پیش‌تر این‌جا بود. یک پاراگرافِ پیوسته و ترازشده از دو
 * طرف، به درخواستِ خودش.
 *
 * ── جدولِ کوتاه ──
 * ایده از سایتی که بابک نشان داد: چهار واقعیتِ فشرده کنارِ معرفی. ولی
 * هیچ عددِ تازه‌ای ساخته نشد — هر چهار خانه از چیزی می‌آید که سارا
 * پیش‌تر داده است. عددهای «تأثیر» (۵۰۰ نفر، ۱۵٬۰۰۰ دلار) عمداً این‌جا
 * تکرار نشدند تا وقتی به آن بخش می‌رسیم تازه بمانند.
 *
 * ── تصویر و زمینه ──
 * بابک خواست از گرافیکِ «ABOUT ME» خودِ سارا استفاده شود و زمینه‌ی این
 * بخش و بخشِ بالایی هم همان رنگ شود. رنگ اندازه‌گیری شد (میانه‌ی پنج
 * ناحیه‌ی تمیزِ `Final.png`): **#090a09**، با انحرافِ معیارِ ۱.۸ یعنی
 * عملاً تخت. توکنش `onyx` است.
 *
 * **اشتباهی که کردم:** اول سارا را از کارت برش زدم. منظورِ بابک همین
 * کارت به‌صورت کامل بود، «بدون هیچ تغییری». حالا همان `Final.png` است
 * با ابعادِ اصلی. برچسبِ «درباره» بالای بخش هم به‌خواستِ او برداشته شد.
 *
 * نکته‌ای که باید بداند: خودِ کارت یک بیوگرافیِ **انگلیسی** دارد که
 * همان حرفِ پاراگرافِ فارسیِ کنارش است. عمداً دست نزدم چون گفت تغییری
 * ندهم، ولی دو زبانِ یک متن کنارِ هم می‌نشینند.
 */

const FACTS = [
  { k: "نقش", v: "کارآفرین · رهبر · مربی" },
  { k: "سابقه", v: "۱۲ سال" },
  { k: "زمینه", v: "بازاریابی شبکه‌ای و رهبریِ سازمان" },
  { k: "تمرکز", v: "ساختنِ آدم‌هایی که خودشان رهبر می‌شوند" },
];

export default function AboutSara() {
  return (
    <SectionShell id="about" tone="onyx">
      {/* در RTL ستونِ اول راست است، پس متن سمتِ شروعِ خواندن می‌نشیند و
          عکس دنبالش می‌آید. */}
      <div className="grid items-start gap-[clamp(28px,5vw,72px)] lg:grid-cols-[0.92fr_1.08fr]">
        <div>
          <h2 className="reveal font-display text-[clamp(24px,3.4vw,42px)] font-extrabold leading-[1.26] tracking-tight text-chalk">
            سارا داودی‌فر
          </h2>

          {/* تراز از دو طرف (`text-justify`) خواسته‌ی بابک است. در فارسی
              خط‌فاصله‌گذاری نداریم، پس مرورگر فقط فاصله‌ی بینِ کلمه‌ها را
              می‌کشد؛ روی ستونی به این عرض قابل‌قبول است، ولی اگر ستون
              باریک‌تر شود «رودخانه»ی فاصله پیدا می‌شود. به همین دلیل در
              موبایل تراز به راست برمی‌گردد. */}
          <p className="reveal mt-[clamp(20px,3.5vh,38px)] max-w-[58ch] text-[clamp(14.5px,1.45vw,16.5px)] leading-[2.15] text-dust sm:text-justify">
            من سارا هستم؛ یک کارآفرین، رهبر، مربی و فعال حوزه توسعه فردی، با ۱۲
            سال تجربه در بازاریابی شبکه‌ای و پیشینه‌ای دیرینه در ورزش. مسیر زندگی
            من با تاب‌آوری، یادگیری مستمر، تجربه در کسب‌وکار و تعهد عمیق به رشد
            افراد در ابعاد فردی، حرفه‌ای و مالی شکل گرفته است. من یک سازمان بزرگ
            را ساخته و رهبری کرده‌ام، سرمایه‌گذاری قابل‌توجهی برای رشد و توسعه
            فردی خود انجام داده‌ام و اکنون در حال ساخت یک برند شخصی چندبعدی هستم
            که رهبری، کسب‌وکار، رشد فردی، توانمندسازی مالی و ورزش را در کنار
            یکدیگر قرار می‌دهد. مأموریت من ساده است: رشد کردن، رهبری کردن و کمک
            به دیگران برای تبدیل شدن به بهترین و قدرتمندترین نسخه خودشان.
          </p>

          <dl className="reveal mt-[clamp(30px,5vh,58px)] grid gap-px overflow-hidden rounded-xl bg-white/10 sm:grid-cols-2">
            {FACTS.map((f) => (
              <div className="bg-[#121312] p-[clamp(16px,1.8vw,22px)]" key={f.k}>
                {/* بی `/70`: در ۱۱ پیکسل نسبتِ تضاد ۳.۶۱:۱ می‌شد، زیرِ آستانه‌ی
                    ۴.۵. برچسب‌بودنِ این کلمه از اندازه و فاصله‌ی حروف
                    می‌آید، نه از کم‌رنگ‌کردنش. */}
                <dt className="text-[11px] tracking-[0.14em] text-dust">{f.k}</dt>
                <dd className="mt-2 text-[clamp(14px,1.4vw,16px)] leading-[1.75] text-chalk">
                  {f.v}
                </dd>
              </div>
            ))}
          </dl>
        </div>

        {/* گرافیکِ «ABOUT ME» خودِ سارا، **دست‌نخورده**. اولین بار سارا را
            از آن برش زدم؛ بابک گفت منظورش همین کارت به‌صورت کامل بوده.
            پس همان `Final.png` است با ابعادِ اصلیِ ۱۶۷۸×۹۳۷، فقط برای
            وزن به webp رفت (۱۶۰۰KB → ۱۳۳KB، اختلافِ پیکسلیِ میانگین ۱.۰
            از ۲۵۵ یعنی چشم تفاوتی نمی‌بیند).

            قاب ۱.۷۹۱ است، دقیقاً نسبتِ خودِ فایل، تا هیچ‌جای کارت بریده
            نشود. `object-contain` هم برای همین: حتی اگر قاب در عرضی کمی
            فرق کند، کارت کامل می‌مانَد.

            زمینه‌ی بخش همان #090a09 است که از خودِ این فایل اندازه گرفته
            شد، پس لبه‌ای بین کارت و صفحه دیده نمی‌شود. */}
        <div className="reveal relative order-first aspect-[1678/937] lg:order-none">
          <Image
            alt="کارتِ معرفیِ سارا داودی‌فر"
            className="object-contain"
            fill
            quality={92}
            sizes="(min-width: 1024px) 50vw, 100vw"
            src={asset("/images/about-me-card.webp")}
          />
        </div>
      </div>
    </SectionShell>
  );
}
