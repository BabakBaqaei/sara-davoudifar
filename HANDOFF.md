# وضعیت پروژه — سایت پرسنال برندینگ سارا داودی‌فر

آخرین به‌روزرسانی: ۲۰۲۶-۰۹-۱۰ (ویدیوی قرمزِ بابک)

## از کجا شروع کنم

۱. این سند را کامل بخوان — همه‌ی دام‌ها و دلیلِ عددها این‌جاست.
۲. `npm run dev -- -p 3100` و بعد `localhost:3100`.
۳. چهار هیرو با `?hero=1|2|3|4` سوییچ می‌شوند؛ **سارا نسخه‌ی ۳ (ویدیو) را انتخاب کرد** و همان پیش‌فرض است.
۴. منابعِ ساختِ دارایی‌ها در `build-assets/` است، نه در پوشه‌ی موقت.

**کارهای باز:** استقرار، لینک‌های واقعی شبکه‌های اجتماعی و ایمیل، رضایتِ
نفر دومِ عکسِ `g-mastermind-turkey`، و جای تازه‌ی کارت ویدیو
(`hero-video-card.tsx` فعلاً هیچ‌جا استفاده نمی‌شود).

## اجرا

```bash
cd "C:/Users/BABAK/OneDrive/Desktop/babak 01/sara-site" && npm run dev -- -p 3100
```

اگر پورت اشغال بود: `netstat -ano | findstr :3100` و بعد `taskkill /PID <pid> /F`.
اگر ماژولی «پیدا نشد» ولی فایلش موجود است: پوشه‌ی `.next` را پاک کن (کش Turbopack).

## پشته

- Next.js 16.3.3 (App Router + Turbopack)، React 19.2.8، TypeScript
- Tailwind CSS **v4** — پیکربندی داخل `src/app/globals.css` با `@theme`. **هیچ `tailwind.config.js` وجود ندارد.**
- `motion/react`، ساختار shadcn (`src/components/ui`، `src/lib/utils.ts`)
- RTL: `dir="rtl" lang="fa"` روی `<html>`

## ساختار صفحه

`src/app/page.tsx`:
```
<HeroSplit />   → نقاشی تمام‌عرض + متن + کارت ویدیو
<Story />       → روایت
<StageSphere /> → گالری کروی
<Contact />     → تماس
```

## کامپوننت‌ها

| فایل | نقش |
|---|---|
| `src/components/sections/hero-split.tsx` | هیرو؛ ۴ لایه: نقاشی → بلر ماسک‌شده → تینت گرادیانی → متن. مودال پخش ویدیو هم اینجاست. |
| `src/components/sections/hero-video-card.tsx` | کارت ویدیو. **فعلاً هیچ‌جا استفاده نمی‌شود** — بابک گفت از هیرو برداشته شود تا بعد بگوید کجا برود. کامپوننت و `public/video/*` دست‌نخورده‌اند. |
| `src/components/sections/story.tsx` | بخش روایت |
| `src/components/sections/stage-sphere.tsx` | بخش گالری. داده‌ی تصاویر و **نقطه‌ی کانونی** هرکدام این‌جاست. |
| `src/components/sections/contact.tsx` | تماس |
| `src/components/ui/shine-border.tsx` | بوردر درخشان (پورت‌شده از v3 به v4) |
| `src/components/ui/gallery-animation.tsx` | گالری کشسان — قاب زیر نشانگر باز می‌شود، کلیک مودال را می‌آورد. |
| `src/components/ui/video-backdrop.tsx` | ویدیوی پس‌زمینه‌ی نیم‌شفاف. |
| `src/components/ui/page-shine-frame.tsx` | قاب درخشان ثابت دور کل صفحه. |
| `src/components/ui/img-sphere.tsx` | کره‌ی تصاویر. **بازنشسته** — جایش گالری کشسان آمد. |
| `src/components/ui/social-icons.tsx` | داک شبکه‌های اجتماعی |
| `src/components/ui/tailwind-css-background-snippet.tsx` | `RadialBackground` + `Hero` |

## وضعیت هیرو — آشکارسازی سینماتیک (تأیید‌شده روی مرورگر)

پرتره‌ی تمام‌کادر روی زمینه‌ی قرمز، و یک «عدسی» گرد که دنبال نشانگر
ماوس می‌آید و نسخه‌ی روشن را روی نسخه‌ی تاریک باز می‌کند — مثل نوری که
روی صورت می‌چرخد.

پارامترها عیناً از کامپوننت `CinematicHoverMaskReveal` در ویدیوی مرجع:

| پارامتر | مقدار |
|---|---|
| Mask Size | ۵۰.۱٪ ارتفاع کادر (نه پیکسل ثابت) |
| Edge Softness | ۹.۳٪ ارتفاع کادر |
| Initial X / Y | ۵۱٪ / ۵۵٪ — روی صورت، نه مرکز کادر |
| Reveal Speed | 0.18 |
| Overlay Tint / Opacity | `#000000` / 0.12 |
| Image Fit | cover |

چیدمان (قرینه‌ی مرجع برای RTL):

- **راست** — `<h1>` نام، نقش، جمله‌ی سه‌خطی، دکمه‌ی «شروع گفت‌وگو»
- **چپ** — عمداً خالی؛ صورت این‌جاست

پیل «Personal Brand»، بلوک آمار، و خط جداکننده‌ی بالای جمله به‌خواستِ بابک
برداشته شدند و گرید از سه ستون به دو ستون رسید.

**اندازه‌ی ماسک عمداً نسبی است، نه پیکسل ثابت.** با `object-fit: cover` و
کادری که نسبتش از تصویر کم‌تر است، تمام ارتفاع تصویر روی ارتفاع کادر
می‌افتد؛ یعنی صورت همیشه کسر ثابتی از ارتفاع کادر است و روی نمایشگر
بزرگ‌تر بزرگ‌تر رندر می‌شود. عدد پیکسلی ثابت آن‌جا نسبتاً آب می‌رفت.

صورت سارا در ترکیب‌بندی فعلی: پیشانی ۱۳٪ · چانه ۶۵٪ — یعنی پیشانی‌تا‌چانه
۵۲٪ ارتفاع کادر، مرکز صورت روی (۵۱٪، ۳۹٪).
در ۱۹۹۰×۱۱۳۰ (کادر ۲۲۵۱×۱۲۸۸): هسته‌ی کاملاً روشن ۲۷۴px، قطر مؤثر ۷۱۰px،
صورت ۶۷۰px → ۱.۰۶×. بدون خطای کنسول، بدون سرریز افقی.

**زوم‌اوت یک‌بار اعمال و بعد برگشت — و دو تکه بود، نه یک تکه.** این دام
یک‌بار خورده شد: فقط تکه‌ی دوم برگشت و کار «تمام» اعلام شد، در حالی که
بابک هنوز زوم‌اوت را می‌دید.

| تکه | زوم‌اوت | برگشت |
|---|---|---|
| ۱ | `artScale` از `[1.14, 1.2]` به `[1, 1.08]` | زومِ سکون رفت به چیدمان: `lg:-inset-[7%]` روی کادر، `artScale` شد `[1, 1.053]` |
| ۲ | تصویر با ضریب ۰.۷۴ از نو ترکیب شد؛ ماسک `0.501`/`0.093`، مرکز ۵۱٪/۵۵٪ | دارایی‌ها از `aligned-*` بازساخته شد؛ ماسک `0.676`/`0.125`، مرکز ۵۱٪/۳۹٪ |

**چرا زومِ سکون در چیدمان است و نه در `transform`:** کامپوننت آشکارسازی
اندازه‌ی ماسک و نقطه‌ی سکون را از `root.getBoundingClientRect()` می‌گیرد و
آن rect **بعد از** اعمالِ transformِ والد است. پس px هایی که روی لنز
می‌نویسد یک‌بار دیگر هم با همان والد بزرگ می‌شوند: با `scale:1.14` ماسک
۱.۱۴ برابرِ لازم می‌شد (۱.۲۱× صورت به‌جای ۱.۰۶×) و نقطه‌ی سکون حدود ۷۰px
زیرِ صورت می‌افتاد. با `-inset-[7%]` خودِ کادر ۱.۱۴ برابر می‌شود، rect
واقعی است و همه‌ی نسبت‌ها سرِ جای خود می‌مانند. `overflow-hidden` بخش
لبه‌ها را می‌برد. `artScale` فقط پارالاکسِ اسکرول مانده (۱.۲/۱.۱۴ = ۱.۰۵۳).

نوارِ موبایل (`h-[58svh]`) دست نخورده: آن‌جا `object-cover` روی کادرِ عمودی
خودش زوم می‌کند و ۷٪ اضافه لازم ندارد.

دارایی‌ها از `build-assets/aligned-{lit,dark}.jpg` دوباره ساخته می‌شوند — آن
دو فایل منبعِ اصلیِ هم‌ترازشده‌اند و نباید پاک شوند. **و خودشان همان
کلوزآپِ اولیه‌اند**، نه فریمِ بازِ زوم‌اوت؛ یک‌بار به اشتباه ۰.۷۴ از آن‌ها
برش زده شد و سرِ سارا کامل از کادر بیرون افتاد.

**اگر عکس هیرو را عوض کردی، این نسبت‌ها را دوباره حساب کن:**
`maskHeightRatio = (۱.۰۶ × کسر ارتفاعِ پیشانی‌تا‌چانه) / (۱ − ۰.۱۸۵)` و
`edgeSoftnessRatio = ۰.۱۸۵ × maskHeightRatio`. آن ۰.۱۸۵ نسبتِ نرمی به قطر
است و ثابت می‌ماند تا حسِ لبه با تغییر زوم عوض نشود. `initialX/Y` را هم
روی مرکز صورت بگذار، نه مرکز کادر.

### دارایی‌های آشکارسازی

فعال: `hero-dark-{2560,1792,1280}.jpg` پایه و `hero-lit-*.jpg` رونما، ساخته از
`ax sara/01.jpeg` (روشن) و `ax sara/03.jpeg` (سیلوئت).

**این دو منبع هم‌تراز نبودند** — دو تولید جداگانه با IoU سیلوئت ۰.۸۱: سارا در
`03` حدود ۱۰٪ کوچک‌تر و پایین‌تر بود. با جست‌وجوی شبکه‌ای مقیاس و انتقال روی
ناحیه‌ی سر هم‌تراز شدند (`build-assets/align2.py`, `verify.py`):

| | قبل | بعد |
|---|---|---|
| IoU سرِ سیلوئت | ۰.۷۷۸ | **۰.۹۸۰** |
| انحراف لبه‌ی چپ (میانه) | ۲۷px | **۲px** |
| انحراف لبه‌ی راست (میانه) | ۴۲px | **۱px** |

تبدیل هم‌ترازی: مقیاس ۱.۱۵۵ روی `03` با انتقال (۱۶، ۶) در ۲۷۵۲×۱۵۳۶.

#### زوم‌اوت — چرا با CSS ممکن نبود

نسبت تصاویر ۱.۷۹ و نسبت کادر هیرو ۱.۷۵ است، پس `object-cover` همین حالا
تقریباً تمام عرض تصویر را نشان می‌دهد و جای عقب‌رفتن ندارد. زوم‌اوت لازم
داشت خودِ تصویر از نو ترکیب شود: سارا کوچک‌تر روی کادر، با زمینه‌ی امتدادیافته.

زمینه با یک **رادیال بیضوی** مدل شد — مرکز (۵۰٪، ۵۰٪)، نسبت محور ۰.۶،
پروفایل شعاعی از میانه‌ی خودِ پیکسل‌های زمینه، و امتداد بیرونی با شیب
انتهایی و میرایی نمایی. برازش **RMSE ۳.۱۱ از ۲۵۵** (حدود ۱٪)، پس امتداد
بی‌درز است. ضریب زوم فعلی **۰.۷۴**.
(`build-assets/bgfit.py`, `zoomout2.py`, `final.py`)

سوژه **پایین‌چسبان** نشانده می‌شود (`oy = H − H·k`) تا تن از لبه‌ی پایین بزند
بیرون. اگر بالاتر بنشیند، لبه‌ی پایینِ عکسِ منبع وسط کادر می‌افتد و پلیور با
یک بُرش صاف در زرشکی معلق می‌ماند. عارضه‌اش این است که هرچه زوم کمتر، سارا
پایین‌تر می‌آید — ذاتیِ همین قید است.

**سوژه‌ی مات را جمعی مدل نکن.** اول `تصویر = زمینه + جزئیات` گرفتم و پوست
سرخابی شد: سوژه زمینه را جانشین می‌کند نه جمع، و لایه‌ی تفاضل که با آفست
۱۲۸ ذخیره شده بود کانال سبزِ پوست (۱۴۸) را می‌برید. راه درست ترکیب آلفاست:
`premult = img − bg×(1−α)` و بعد `out = new_bg×(1−α) + premult`. این فرمول
سوژه و لبه‌ی نرم و نشتِ نور لبه‌ای را یک‌جا می‌برد. هر جزء را جدا و در بازه‌ی
سالم [۰،۲۵۵] مقیاس بده، بعد ریاضی را روی float انجام بده.

**هم‌ترازی را روی ناحیه‌ی سر بسنج، نه کل سیلوئت** — پهنای تن و شانه‌ها بر
معیار غالب می‌شوند و صورت می‌تواند جابه‌جا بماند. و با همبستگی بافتِ صورت
هم نسنج: صورت در نسخه‌ی تاریک بی‌بافت است (روشنایی ۹.۲) و چیزی برای
همبسته‌کردن ندارد؛ معیار درست، مرزِ سیلوئت و نور لبه‌ای است.

استفاده‌نشده: `02.jpeg` — همان نسخه‌ی تاریک با برش پهن‌تر (۲۴۰۰×۱۷۹۲). برای
موبایل که برش مرکزی فقط ۴۴٪ عرض را نشان می‌دهد مناسب بود، ولی جفتِ روشنِ
هم‌نسبت ندارد، پس قابل استفاده در آشکارسازی نیست.

بازنشسته: `portrait-*.jpg` (ساخته از عکس واقعی `IMG_9963.JPG` با rembg و
نورپردازی مصنوعی) و `reveal-*.jpg` (جفت AI اول).

## چند هیرو، یک سایت

هیرو با `?hero=<کلید>` انتخاب می‌شود و بقیه‌ی صفحه بین همه مشترک است.
`searchParams` در این نسخه‌ی Next یک Promise است و سمت سرور خوانده می‌شود،
پس سوییچ بدون فلیکر و بدون JS اضافه است (و صفحه را داینامیک می‌کند — برای
مرحله‌ی ارزیابی مشکلی نیست).

| کلید | فایل | حال‌وهوا |
|---|---|---|
| `1` | `sections/hero-split.tsx` | سینماتیک؛ پرتره‌ی تمام‌صفحه‌ی تاریک، نور متعقب ماوس |
| `2` | `sections/hero-v2-editorial.tsx` | تصویر «ABOUT ME» تمام‌صفحه، بدون هیچ لایه‌ی متنی |
| `3` (**پیش‌فرض**) | `sections/hero-v3-video.tsx` | ویدیوی سارا، تمام‌صفحه، بدون هیچ متن یا لایه |
| `4` | `sections/hero-v4-layered.tsx` | همان گرافیک «ABOUT ME»، ولی هشت لایه‌ی زنده با GSAP + ScrollTrigger |

افزودن هیروی بعدی: یک فایل در `sections/` و یک خط در
`components/heroes/registry.ts`. **هیچ هیروی موجودی دست نمی‌خورد.**
دارایی هر نسخه با پیشوند خودش ذخیره می‌شود (`hero2-*`, `hero3-*`,
`hero4/*`).

`components/heroes/hero-switcher.tsx` نوارِ پایین صفحه است برای رفتن بین
نسخه‌ها. **ابزار ارزیابی است** — در سایت نهایی این کامپوننت و فراخوانی‌اش
در `page.tsx` حذف می‌شوند.

### هیروی ۲ — تصویر «ABOUT ME»

`sections/hero-v2-editorial.tsx` (نام فایل از نسخه‌ی قبل مانده؛ کامپوننت
`HeroAbout` است).

خروجی: `hero2-about-{2560,1792,1280}.webp` (۲۳۹/۱۲۹/۷۳KB).

**طراحی از گرافیکِ بابک است، ولی خودِ سارا از عکس خام.** بابک گفت چهره‌ی
سارا در نسخه‌ی قبل «انگار یک لایه و افکت رویش هست». درست بود: منبعِ قبلی
`ax sara/2322.jpg_2K_202609071651.jpeg` یک آپ‌اسکیلِ AI بود و پوست را
پلاستیکی و چشم‌ها را بی‌جزئیات می‌کرد.

ارتفاع صورت به پیکسلِ واقعی — همین عدد تصمیم را گرفت:

| منبع | ارتفاع صورت |
|---|---|
| `ax sara/2322.jpg` (عکس خام، ۳۰۲۴×۴۰۳۲) | **~۴۷۰px** |
| `2322.jpg_2K_…` (نسخه‌ی قبلی سایت) | ~۲۷۰px |
| `ChatGPT Image Sep 7 …png` (گرافیکِ تمیزِ ۱۶۷۲×۹۴۰) | ~۱۰۰px |

گرافیکِ تمیز در کل بهتر دیده می‌شود ولی کم‌ترین پیکسلِ واقعی را دارد و در
۲۵۶۰ عرض از نسخه‌ی قبلی هم نرم‌تر می‌شد. پس **طراحی دست نخورد** — زمینه،
«ABOUT ME»، امضا، پاراگراف و برچسب‌ها همان `2322.jpg_2K` ماندند — و فقط
پیکسل‌های سارا از عکس خام آمد، که اتفاقاً منبعِ خودِ همان گرافیک است.
شارپی صورت **۲.۰۵ برابر** شد (واریانس لاپلاسین ۱۳۰.۸ → ۲۶۸.۴).

هم‌ترازی با IoU سیلوئت روی سر و بالاتنه: **۰.۸۹۰۵** (مقیاس ۰.۷۴۴۶، آفست
۲۳۲/−۱۱۰). کامل نیست چون عکس خام از فاصله‌ی نزدیک و از پایین گرفته شده و
شانه‌ها نسبت به سر پهن‌تر می‌افتند؛ ۰.۶۳٪ کادر حفره ماند که با پرکردنِ
افقی از زمینه‌ی خودِ تصویر پوشیده شد.

**دامِ این کار:** امضا و پاراگراف در طراحی *روی* سارا کشیده شده‌اند. سارای
نو پهن‌تر است، پس بعد از جای‌گذاری زیرِ او افتادند و امضا کامل ناپدید شد.
`retype.py` آن‌ها را از خودِ تصویرِ قبلی بیرون می‌کشد و دوباره رو می‌گذارد.
جداسازی با «قلمِ نازک» انجام می‌شود نه با روشنایی: شلوارِ سفید هم روشن و
بی‌رنگ است، ولی در پنجره‌ی ۲۵ پیکسلی کسرِ روشنش ۱ است و قلمِ امضا کم.

**دو مسیری که امتحان شد و جواب نداد:**
- قابِ سارا از نسخه‌ی فعلی + طراحیِ گرافیک جدید. تایپِ دو طراحی یکی نیست
  («ABOUT ME» در نسخه‌ی فعلی حروفِ کوتاه‌وپهن دارد، در جدید بلندوباریک —
  هم‌عرض‌کردن ارتفاعش را از ۴۲٪ به ۵۷٪ می‌برد). با بزرگ‌کردنِ سارا «T ME»
  و وسطِ امضا زیر او گم شد.
- برشِ ۰.۷۴ از `aligned-*`-وار روی گرافیک. اندازه‌گیریِ «بلوز قرمز» با
  رژِ لبِ سارا آلوده شده بود و سرش کامل از کادر بیرون افتاد. اگر باز
  اندازه‌گیری لازم شد، بلوز را با **اجتماعِ نواحی پیوسته‌ی بزرگ‌تر از ۱۰٪
  بزرگ‌ترین** بگیر، نه با جعبه‌ی همه‌ی پیکسل‌های قرمز.

**یک ادعای غلط که ثبت شود تا تکرار نشود:** یک‌بار نتیجه گرفتم نسخه‌ی ۲K
سارا را ۱.۳۷ برابر پهن کرده (نسبت عرض/ارتفاعِ بلوز ۱.۱۴۶ در برابر ۰.۸۴).
غلط بود — با نسبتِ سر بررسی شد (۱.۰۸۴ در برابر ۱.۳۳۶ عکس خام) و معلوم شد
اختلاف از پرسپکتیوِ عکسِ نزدیکِ موبایل است، نه کشیدگی.

تیتر، امضا، پاراگراف و فوتر همه **داخل خودِ تصویر** است، پس هیرو هیچ لایه‌ی
متنی ندارد — هر متنی بگذاریم روی متنِ تصویر می‌افتد.

`object-contain` در موبایل و `object-cover` در دسکتاپ: نسبت تصویر ۱.۷۹۲ و
نسبت هیرو در دسکتاپ ~۱.۷۶ است پس برش ناچیز؛ ولی در موبایل کادر عمودی
می‌شود و با cover متنِ داخل تصویر بریده می‌شود.

**سرور-کامپوننت است** — نه تعامل دارد و نه استیت، پس `use client` ندارد و
از باندل کلاینت بیرون می‌مانَد.

نسخه‌ی قبلی (کینتیک ادیتوریال: کارت پرتره‌ی سه‌بعدیِ متعقب ماوس، اسمِ
غول‌پیکر با گرادیانِ متحرک، نشانگر اسکرول) کامل حذف شد، همراه دارایی‌اش
`hero2-portrait.webp`.

### هیروی ۳ — ویدیوی سارا، تمام‌صفحه (هیروی پیش‌فرض)

کلیپِ خودِ بابک، تدوین‌شده بر اساس هیروی مرجع ellieswift.com. هیچ متن،
تیتر، گرادیان یا لایه‌ی رویی ندارد.

منبع: `ax sara/Sara_adjusting_scarf_in_video_202609071026.mp4`
(۱۹۲۰×۱۰۸۰، ۸s، ۲۴fps). خط لوله در `build-assets/edit.py`.

#### تحلیلی که تدوین را تعیین کرد

واریانس لاپلاسینِ ناحیه‌ی سوژه در طول کلیپ، سه فاز را لو داد:

| بازه‌ی منبع | تیزی | چه خبر است |
|---|---|---|
| ۰–۱.۲۵s | ۱۰۰–۱۷۵ | ورود، تارِ حرکتیِ طبیعی |
| ۳.۴–۵.۵s | **۳۷۰–۴۳۱** | مکث و لبخند، تیزترین لحظه |
| ۶.۵–۷.۹s | ۹۴→۱۵ | خروج سریع |

#### کادر — زوم، و سارا در نیمه‌ی چپ

پنجره‌ی ۱۶:۹ به عرض **۱۲۰۲** با **لبه‌ی راست روی لبه‌ی راستِ منبع**، از بالا
لنگرانداخته: `crop=1202:676:718:0`.

لبه‌ی راست عمداً ثابت است تا ورودِ سارا از راست حفظ شود. و همین که عرض
کم‌تر می‌شود، **سه چیز را با هم** می‌دهد — این‌ها به هم قفل‌اند، نه سه
تنظیم مستقل:

| عرض کادر | برش پایین | موقعیت سارا در مکث | کامل خارج می‌شود |
|---|---|---|---|
| ۱۴۹۶ | ۷۸٪ | ۵۵.۷٪ (نیمه‌ی راست) | ۷.۹۴s |
| **۱۲۰۲** | **۶۲.۶٪** | **۴۴.۹٪ (نیمه‌ی چپ)** | **۷.۶۱s** |
| ۱۱۰۰ | ۵۷.۳٪ | ۳۹.۸٪ | ۷.۵۳s |

**هزینه‌اش:** خروجی ۱۲۰۲×۶۷۶ است و مرورگر آن را ۱.۲۱× بزرگ می‌کند تا کادر
۱۴۴۰ را پر کند. اجتناب‌ناپذیر است — منبع ۱۹۲۰ عرض دارد و برای بردنِ سارا
به نیمه‌ی چپ باید سمت راست بریده شود.

#### نقشه‌ی زمانی

| بازه‌ی منبع | سرعت | طول خروجی |
|---|---|---|
| ۰.۱۰–۱.۹۰ | ۱.۰۰× | ۱.۸۰s — ورود |
| ۱.۹۰–۳.۳۰ | ۱.۰۰× | ۱.۴۰s |
| ۳.۳۰–۵.۶۰ | **۰.۶۲×** | ۳.۷۱s — مکث و لبخند، اسلوموشن |
| ۵.۶۰–۶.۵۵ | ۱.۰۰× | ۰.۹۵s |
| ۶.۵۵–۷.۹۴ | ۱.۰۰× | ۱.۳۹s — خروج کامل |

خروجی: **۱۲۰۲×۶۷۶، ۳۰fps، ۹.۲۷s**. فریم‌های میانیِ اسلوموشن با آمیختنِ
خطیِ دو فریم مجاور ساخته می‌شوند؛ در مکث حرکت کم است پس آمیختن به‌اندازه‌ی
درون‌یابیِ نوری نرم درمی‌آید.

**درزِ لوپ خودش حل شد.** سارا در ثانیه‌ی ۷.۶ از لبه‌ی چپِ کادرِ تنگ‌تر
بیرون می‌رود و ۰.۳ ثانیه کادرِ خالی می‌مانَد؛ فریم اول هم تقریباً خالی است
(او تازه از راست پیداست). پس خالی به خالی وصل می‌شود.

دو نسخه‌ی قبل کنار گذاشته شد: تندکردنِ خروج (۱.۷×) که حرکت را مصنوعی
می‌کرد، و کوتاه‌کردنش که باعث می‌شد سارا وسطِ کادر یک‌دفعه محو شود.

#### تاری ورود

رمپ گاوسی ۲۲px → ۰ روی ۱.۹ ثانیه‌ی اول خروجی، با منحنی `(1-p)^1.8`.
تأیید شد: تیزیِ خروجی ۰ → ۷۴ در همان بازه.

**بیتِ خالیِ ابتدای کلیپ عمداً نگه داشته شد.** سارا از چپ خارج می‌شود، یک
لحظه کادر خالی می‌ماند، بعد از راست وارد می‌شود — همان بیت درزِ لوپ را
طبیعی می‌کند و حذفش لوپ را بدتر می‌کند.

| فایل | حجم |
|---|---|
| `sara-hero.mp4` | ۱۵۰۴KB — H.264 |
| `sara-hero.webm` | ۶۷۶KB — VP9 |
| `sara-hero-poster.jpg` | ۳۹KB — از میانه‌ی مکث |

#### متن روی هیرو

سه بلوک در سمت **راستِ فیزیکی**، همه با فونت پیدا:

```
سارا / داودی‌فر            font-display, وزن ۸۰۰
                           موبایل clamp(40px,9vw,88px) · دسکتاپ clamp(44px,5.2vw,88px)
کارآفرین · مربی · رهبر سازمان   clamp(11px,1.05vw,13.5px), tracking .14em
انضباط را از ورزش آوردم
به کسب‌وکار سپردم           clamp(13px,1.25vw,17px), line-height 2.1
و حالا به آدم‌ها می‌سپارم
```

**جای ستون از اندازه‌گیری آمد، نه از حدس.** سارا در طول مکث x را از ۲۵٪ تا
**۷۱٪** کادر می‌گیرد (سنجیده با rembg روی فریمِ رندرشده‌ی هیرو، نه روی خودِ
ویدیو — نسبتِ کادر برش می‌زند و عددها را جابه‌جا می‌کند).

**حاشیه‌ی راست با عرضِ پنجره رشد می‌کند و ثابت نیست:** `clamp(48px,6.5vw,150px)`.
نوارِ خالیِ راست با پهن‌شدنِ پنجره بزرگ‌تر می‌شود؛ با حاشیه‌ی ثابتِ ۷۱px متن
به لبه می‌چسبید و یک فاصله‌ی هدررفته بین سارا و متن می‌ماند — بابک همین را
دید. با این نسبت، بلوک تقریباً وسطِ نوار می‌نشیند:

| عرض | حاشیه‌ی راست | فاصله‌ی چپِ بلوک | نامِ |
|---|---|---|---|
| ۱۴۲۵ | ۹۴px | ۴۱px | ۷۵px |
| ۱۹۹۰ | ۱۲۹px | ۱۳۹px | ۸۸px |

در ۱۹۹۰ مقدارِ ایده‌آل برای وسط‌نشستنِ کامل ۱۳۴px بود، پس ۵px خطا دارد.

**نرخِ رشدِ اندازه‌ی نام (`5.2vw`) هم به همین قید بسته است، نه به سلیقه.**
نوارِ خالی ۲۹٪ عرض است و نام + حاشیه باید در آن جا شود. با `5.6vw` نام در
عرض ۱۴۲۵ تا ۲۱px به شال سارا می‌رسید؛ با `5.2vw` فاصله ۴۱px شد.

عرض ستون `clamp(300px,32vw,520px)` — فقط قیدِ شکستنِ خط است، چون متن
راست‌چین است و هر خط به‌اندازه‌ی محتوایش عرض می‌گیرد.

**رنگ‌ها روی دیوارِ روشن سنجیده شدند.** نوار متن یکدست است (`#d9cfc0`،
روشنایی ۲۰۶ از ۲۵۵) پس متن تیره است:

| بلوک | رنگ | کنتراست با دیوار |
|---|---|---|
| نام | `#241c17` | ۱۰.۸۸:۱ |
| نقش | `#463b32` | ۷.۰۶:۱ |
| جمله | `#33291f` | ۹.۲۳:۱ |

نسخه‌ی اول نقش را `#5c4f45` گذاشته بود که ۵.۱۳:۱ می‌داد — از AA رد می‌شد
ولی برای متنِ ریزِ فاصله‌دار کم بود، پس تیره‌تر شد.

**پرده‌ی کنارِ متن تزئینی نیست.** سارا هنگام ورود (۰.۳ تا ۱.۲s) و لحظه‌ای
در خروج از همان نوار عبور می‌کند؛ بی پرده، متن روی شالِ روشنش کنتراست
کافی ندارد. گرادیانِ هم‌رنگِ دیوار از راست، و در موبایل از پایین.

**دو دام RTL که هر دو یک‌بار خورده شد:**
- `right-0` و نه `end-0` — محور منطقی برعکس می‌شود و ستون به چپ می‌افتد.
- `items-start` و نه `items-end` — در RTL محورِ عرضیِ فلکس از راست شروع
  می‌شود، پس «start» راستِ فیزیکی است. با `items-end` تیتر به چپِ جعبه
  چسبید و ۵۰px از padding بیرون زد.
- در دسکتاپ ستون فقط از راست padding می‌گیرد (`lg:px-0 lg:pr-…`): ستون به
  لبه‌ی راست چسبیده و padding چپ فقط عرضِ مفید را می‌خورد و نام را سرریز
  می‌کند.

در موبایل ستون تمام‌عرض و پایین‌چسبان می‌شود (نام ۴۰px)، با حاشیه‌ی راستِ
۲۰px و ۴۹px فاصله از پایین؛ همه‌ی بلوک‌ها داخل کادر و بی سرریز.

**برشِ موبایل.** در دسکتاپ برش ۲.۳٪ است. در موبایل کادر عمودی می‌شود و
تنها ۲۶٪ عرض دیده می‌شود. حالا که سارا در نیمه‌ی چپ است، میانه‌ی موقعیتش
۴۵٪ عرض است و نقطه‌ی برش روی **۴۰٪** بهترین نتیجه را می‌دهد: **۷۵٪** زمان
در کادر، در برابر ۶۷٪ با برشِ مرکزی. **هر بار کادر یا نقشه‌ی زمانی عوض
شد، این عدد را دوباره حساب کن** — با کادر قبلی ۷۰٪ بود. اگر تغییری در برش
یا بازه دادی، این عدد را دوباره حساب کن. برای همیشه‌کامل‌بودن در گوشی،
راهِ درست یک نسخه‌ی عمودی ۹:۱۶ از کلیپ است.

#### آن‌چه پیش‌تر پاک شد

نسخه‌ی ادیتوریالِ کرِم (پرتره‌ی برش‌خورده، تیتر، هایلایتِ ماژیکی) و خط
لوله‌ی ماسک‌زنی‌اش. **درسی که ماند:** آن نسخه از ویدیوی خام یک لوپِ
پایدارشده‌ی ۴ ثانیه‌ای ساخته بود و «مثل عکس» شد — سایه‌ی دیوار در ماسک
مانده بود و پایدارسازیِ زیاده تکانِ طبیعی را گرفته بود. وقتی مشتری
می‌گوید «این ویدیو را بگذار»، همان ویدیو را بگذار؛ پردازشِ خوش‌نیت
می‌تواند همان چیزی را که ارزش داشت از بین ببرد.

منطقِ توقفِ بیرون از کادر: هوک مشترک `lib/use-video-in-view.ts`.

### هیروی ۴ — گرافیک «ABOUT ME» به‌صورت لایه‌های زنده

`sections/hero-v4-layered.tsx` + `lib/hero4/{layers,intro,scroll,parallax,reduced-motion}.ts`
GSAP 3.15 با ScrollTrigger (`npm i gsap`).

همان طراحیِ هیروی ۲، ولی به‌جای یک تصویرِ تخت، هشت لایه‌ی جدا در DOM که
با لود و اسکرول حرکت می‌کنند. دارایی‌ها در `public/images/hero4/`،
مجموعاً ۷۱۵KB در هشت webp تمام‌فریمِ ۲۵۶۰×۱۴۲۹.

**لایه‌های آماده‌ای که بابک فرستاد قابل استفاده نبودند.** هشت PNG در
`Downloads/0*.png` تصویرِ تختِ نهایی را تکه کرده‌اند و هرچه پشتِ سارا
افتاده بود بازسازی نشده:

| فایل | چه داشت |
|---|---|
| `03_about_me_text.png` | فقط «ABOU» + تکه‌ای از T |
| `04_…signature.png` | «Sara» + «udifar» — وسطش («Davo») نبود |
| `02_sara_character.png` | سارا + تکه‌های حروف و متنِ روی او؛ برشِ تخت، نه سوژه‌ی جدا |

در هیروی لایه‌ای که اجزا نسبت به هم حرکت می‌کنند، همین سوراخ‌ها لو
می‌رود — اولین رندر «ABOU» و بعد هیچ نشان داد. پس لایه‌ها از خودِ
`ax sara/2322.jpg_2K_…` استخراج شدند (`build-assets/hero4-*.py`).

**نتیجه‌ی سنجش:** حالتِ سکون با مرجع اختلافِ میانگین **۳.۶۲ از ۲۵۵**
(۱.۹٪ پیکسل با اختلاف >۴۰) دارد — یعنی قیدِ «در پیشرفتِ صفر باید مو‌به‌مو
مرجع باشد» برقرار است.

#### چهار چیزی که در ساخت اشتباه رفت و ثبت می‌شود

۱) **نوارِ حروف.** اندازه‌گیریِ اول گفت «ABOUT ME» تا ۴۷.۹٪ ارتفاع می‌رود؛
   غلط بود چون خودِ اندازه‌گیری در پنجره‌ی ۰..۴۸٪ انجام شده بود و به لبه‌ی
   پنجره خورده بود. حروف واقعاً تا **۵۹.۹٪** می‌روند. اندازه‌گیری را در
   پنجره‌ای نکن که جواب می‌تواند از آن بزند بیرون.

۲) **زمینه‌ی مشکی.** پرکردنِ جای حروف با میانگینِ وزن‌دار در مقیاس ۱/۳۲
   باگ داشت: بلوکی که کاملاً داخلِ یک حرف است هیچ پیکسلِ معتبری ندارد،
   `den` صفر می‌شود و `num/den` مشکی می‌دهد → لکه‌های مشکیِ حرف‌شکل.
   حل: انتشارِ تکراری (۲۰۰ بار تارکردن ۳×۳ در مقیاس ۱/۸) با آستانه‌ی
   اعتبارِ ۹۰٪ روی بلوک‌ها.

۳) **پایین‌تنه‌ی جامانده.** ماتِ rembg در سطر ۱۱۴۹ (۷۴.۸٪) تمام می‌شود
   ولی سارا تا پایینِ کادر ادامه دارد، و پایین‌تنه‌اش در زمینه ماند
   (لکه‌ی قرمز). حل: ادامه‌ی ستونی از لبه‌ی مات + آزمونِ اشباعِ رنگ.

۴) **سارا از عکس خام، رد شد.** برشِ شارپِ عکس خام (که در هیروی ۲ هست)
   این‌جا کار نمی‌کند: سوراخِ لایه‌ی حروف دقیقاً به‌اندازه‌ی سیلوئتِ
   ساراىِ گرافیک است و سیلوئتِ نو با آن IoU ۰.۸۹ دارد → یک شکافِ
   ۱۶۳ پیکسلی کنارِ شانه که با تراوش هم پر نمی‌شد. جای‌گذاریِ فقط صورت
   هم رد شد: بیشینه‌ی همبستگیِ نرمال در ناحیه‌ی صورت **۰.۱۲۸** درآمد،
   یعنی هندسه بر هم نمی‌نشیند. پس سارا در هیروی ۴ همان ساراىِ گرافیک
   است و صورتِ شارپ در هیروی ۲ می‌مانَد. راهِ داشتنِ هر دو، لایه‌های
   واقعیِ همان طراحی است.

#### معماری انیمیشن

سه سیستم، سه عنصرِ تودرتو — چون دو سیستم روی یک عنصر `transform`
همدیگر را پاک می‌کنند:

    .l-par   ← پارالاکسِ ماوس   (parallax.ts، gsap.quickTo)
      .l-scr ← تایم‌لاینِ اسکرول (scroll.ts، pin + scrub 0.8)
        .l-in ← تایم‌لاینِ ورود  (intro.ts، هفت مرحله)

**سنجیده‌شده روی مرورگر:**
* ورود در پیشرفتِ ۱: همه‌ی لایه‌ها `opacity:1` و ترنسفرمِ همانی، پرده‌ی
  امضا `inset(0%)` → مو‌به‌مو مرجع.
* اسکرول: `pin-spacer` ساخته می‌شود، `start 0 / end 1200`. در پیشرفتِ ۱
  حروف `0.94/−۱۰px/۰.۸۵`، سارا `1.06/−۱۰px`، امضا `−۲۵px/۱.۰۲/۰.۹۵`،
  پاراگراف `−۳۵px/۰.۸`، ستاره‌ها `−۲۰px/۰.۵`، برچسب‌ها `∓۲۵px/۰.۶`.
* پارالاکس: جابه‌جایی‌ها **دقیقاً** به نسبتِ عمق‌ها (۲:۴:۷:۳) و در جهتِ
  مخالفِ نشانگر.
* پاک‌سازی: بعد از رفتن به هیروی دیگر، `pin-spacer` صفر و
  `scroll-behavior` برگشته.

**سوراخِ پشتِ سارا چرا لو نمی‌رود:** در اسکرول، حروف و سارا هر دو
`y:-10` می‌گیرند (جابه‌جاییِ نسبیِ عمودی صفر) و سارا فقط بزرگ می‌شود در
حالی که حروف آب می‌روند. اندازه‌گیری: لو‌رفتگی از ۰.۲۴٪ در سکون به
۰.۰۵۵٪ در انتهای اسکرول می‌رسد، یعنی بیشینه‌اش در سکون است — و آن‌جا هم
با پرکردنِ نوارِ نازکِ سکون بسته شده.

**دو انحراف عمدی از پرامپت:** (الف) پروژه‌ی Vite جدا زده نشد؛ خودِ پرامپت
گفته بود «If using React, use a clean component architecture instead» و
این سایت Next است، و هیروی ۴ باید کنار سه هیروی دیگر با همان `?hero=`
قابل مقایسه باشد. (ب) بخشِ `next-section` جعلی با نوشته‌ی «Next Section»
ساخته نشد؛ سایت از قبل بخش‌های واقعی دارد و هیروی پین‌شده که آزاد
می‌شود، بخشِ روایت طبیعی وارد می‌شود.

**قلابِ توسعه:** `section.__hero4 = { intro, scroll, forceParallax, tick }`
فقط وقتی `NODE_ENV !== "production"`. پنلِ مرورگرِ ابزار rAF را throttle
می‌کند و `pointer: coarse` گزارش می‌دهد، پس بی این قلاب نه تایم‌لاین جلو
می‌رود و نه شرطِ پارالاکس رد می‌شود.

**دو محدودیتِ باقی‌مانده:**
* `prefers-reduced-motion` پیاده شده (`reduced-motion.ts`) ولی روی
  مرورگر آزموده **نشده** — پنل راهی برای تقلیدش نمی‌دهد.
* در موبایل، پاراگرافِ داخلِ گرافیک در ۳۷۵px خوانا نیست (هر خط ~۴px).
  این محدودیتِ ذاتیِ گرافیکِ پهنِ متن‌پخته است و هیروی ۲ هم داردش. حلِ
  واقعی، یک ترکیب‌بندیِ جدا برای موبایل است.

### دامی که سه بار تکرار شد

انیمیشن‌های ورودِ motion با `initial={{opacity:0}}` اگر موتور انیمیشن
اجرا نشود، محتوا را در حالت نامرئی رها می‌کنند. در مرورگر واقعی مشکلی
نیست، ولی **هر جا که خوانایی به انیمیشن وابسته شود، باگ است**:

- گالری: پوششِ مودال با `opacity:0` ولی کلیک‌گیر می‌ماند → `pointer-events`
  همان لحظه و مستقیم روی DOM خاموش می‌شود، نه با variant.
- هیروی ۳: کلمه‌ی زیرِ هایلایت اگر کرِم می‌بود، بی‌انیمیشن کرِم‌روی‌کرِم و
  نامرئی می‌شد → کلمه تیره شد (کنتراست ۱۳.۵:۱ بدون نوار هم خوانا).
- `fill-mode: both` هم درمان نیست: حالتِ شروع را در طول تأخیر نگه می‌دارد.

**قاعده: حالتِ سکون باید به‌تنهایی خوانا و کارآمد باشد؛ انیمیشن فقط افزوده.**

## قالب سایت — از پرامپت نمونه‌ی Mainframe

سارا هیروی ۳ (ویدیو) را انتخاب کرد. **هیرو دست نخورد**؛ از آن پرامپت
فقط چیزهای غیرهیرویی برداشته شد و بقیه‌ی سایت با آن ساخته شد.

| از نمونه | این‌جا |
|---|---|
| نوار ثابتِ بالا با نشان، لینک‌های ویرگول‌دار، دعوتِ زیرخط‌دار | `ui/site-nav.tsx` |
| همبرگری که به ضربدر می‌شود + پرده‌ی تمام‌صفحه‌ی تار | همان فایل |
| ردیفِ قرص‌های کنش + قرصِ «رونوشتِ ایمیل» | `ui/action-pills.tsx`، داخل بخش گفت‌وگو |

چیزهایی که عمداً منتقل **نشد**: ویدیوی اسکراب‌شونده با ماوس، برچسبِ تارِ
مقدمه، و افکتِ تایپ‌رایتر — همه‌ی این‌ها اجزای خودِ هیرو بودند.

### چهار انحراف عمدی از پرامپت

۱) **فونت عوض نشد.** پرامپت دو فونتِ Helvetica Now را از CDN می‌آورد.
   این سایت فارسی است و Helvetica هیچ گلیفِ فارسی ندارد؛ قاعده‌ی پروژه
   «همه‌جا پیدا» است و پیدا نیم‌فاصله دارد.

۲) **آینه‌ی RTL.** در نمونه نشان چپ است و دعوت راست. این‌جا `justify-between`
   روی فلکسِ RTL خودش برعکس می‌کند: نشان اولین فرزند → راست (اندازه‌گیری
   شد: x=۱۲۴۱ در عرض ۱۴۴۰)، دعوت آخرین → چپ (x=۳۲)، و لینک‌های میانی با
   x نزولی ۷۲۴ → ۶۰۸ یعنی راست‌به‌چپ. در پرده‌ی موبایل `items-start`
   «راست» است، نه چپ.

۳) **اندازه‌ی قلم کمی پایین آمد** (لینک ۱۷/۱۹px به‌جای ۲۳، نشان ۱۹/۲۳ به‌جای
   ۲۶). گلیف‌های فارسیِ پیدا در همان px بزرگ‌تر دیده می‌شوند و نوار شلوغ
   می‌شد؛ نسبتِ نمونه حفظ شد.

۴) **رنگِ نوار دو حالته شد.** نمونه ویدیوی تیره دارد و متنِ سفید. هیروی ۳
   زمینه‌ی **کرِم** دارد. روی فریمِ واقعیِ ویدیو اندازه‌گیری شد: روشناییِ
   نوارِ بالا ۰.۵۷ تا ۰.۷۷ — متنِ سفید آن‌جا **۱.۴:۱** می‌شد، یعنی
   ناخوانا. با مرکبِ تیره‌ی `#241c17` بدترین حالت **۹.۹۵:۱** است. پایین‌تر
   از هیرو همه‌ی بخش‌ها تیره‌اند و نوار روشن می‌شود (paper، اندازه‌گیری‌شده
   `rgb(242,240,244)`) با `backdrop-blur`.
   رنگِ هر هیرو در **رجیستری** اعلام می‌شود (`navTone`)، حدس زده نمی‌شود.

### دو دامی که همین‌جا خوردم و ثبت می‌شود

**۱) `IntersectionObserver` در پنلِ مرورگرِ ابزار هیچ‌وقت شلیک نمی‌کند.**
تشخیصِ «از هیرو گذشتیم» اولش با IO نوشته شد و نوار روی فوترِ سیاه هم
مرکبِ تیره می‌مانْد، یعنی ناپدید. با یک ناظرِ تازه هم آزمون شد: `timer:
true` ولی `io: false`. جایش `getBoundingClientRect().bottom <= 80` روی
رویدادِ اسکرول آمد — هم قطعی، هم آزمون‌پذیر، هزینه ناچیز.
مرتبط با دامِ «پنل رندر نمی‌کند» که قبلاً هم خورده بودیم.

**۲) ورودِ قرص‌ها با `useState` + `setTimeout` نوشته شد و در پنل با
`opacity: 0` گیر کرد** — در حالی که استایلِ درون‌خطی درست `opacity: 1`
بود. علت: پنل ترنزیشنِ CSS را جلو نمی‌برد. ولی همین نشان داد کد هم
عیب دارد: اگر موتور انیمیشن اجرا نشود، محتوا **نامرئی** می‌مانَد — همان
قاعده‌ای که در این پروژه سه بار شکسته‌ایم. حالا یک کی‌فریمِ CSS است
(`rise-in` در `globals.css`) که **فقط `from` دارد و `fill-mode` ندارد**،
پس بیرون از بازه‌ی انیمیشن عنصر مقدارِ خودش را می‌گیرد: `opacity: 1`.
با `both` یا `backwards` حالتِ شروع نگه داشته می‌شد و باز نامرئی می‌ماند.

### سنجیده‌شده روی مرورگر

* نوار: در عرض ۱۴۴۰ نشان راست/دعوت چپ/لینک‌ها راست‌به‌چپ؛ رنگ روی هیرو
  `#241c17` و پس از هیرو `rgb(242,240,244)` + `blur(12px)`.
* قرص‌ها: ۵ عدد، همه ۴۰px بلند، با x نزولی (۱۰۰۴→۲۸۳) یعنی چیدمانِ RTL
  درست. کنتراست ۲۱:۱ (قرصِ سفید) و ۱۷.۷۵:۱ (قرصِ خط‌دار) — هر دو AAA.
* منوی موبایل: کلیکِ همبرگری شفافیت پرده را ۰ → ۱ می‌کند؛ Escape می‌بندد،
  اسکرولِ صفحه قفل می‌شود، فوکوس به اولین لینک می‌رود.
* `next build` پاس، TypeScript بدون خطا، کنسول بدون خطا.

**نکته‌ی باز:** نشانی `hello@saradavoudifar.com` جای‌نگهدار است و همان را
قرصِ رونوشت هم کپی می‌کند. پیش از انتشار باید واقعی شود.

## معماری روایی — شانزده بخش، یک قصه

سایت از «هیرو + سه بخش» به یک روایتِ پیوسته رفت. ترتیب خودِ قصه است و
جابه‌جا کردنش قصه را می‌شکند:

| # | بخش | فایل | لنگر |
|---|---|---|---|
| — | هیرو (ویدیو) | `hero-v3-video.tsx` | — |
| ۰۱ | جمله‌ی برند | `brand-statement.tsx` | `#statement` |
| ۰۲ | داستان | `the-story.tsx` | `#story` |
| ۰۳ | مسیر | `the-journey.tsx` | `#journey` |
| ۰۴ | چهار جهان | `four-worlds.tsx` | `#worlds` |
| ۰۵ | تأثیر | `impact.tsx` | `#impact` |
| ۰۶ | آدم‌ها | `people-grown.tsx` | `#people` |
| ۰۷ | مربی‌گری | `coaching.tsx` | `#coaching` |
| ۰۸ | شبکه | `network-marketing.tsx` | `#network` |
| ۰۹ | ورزش ۲.۰ | `sport-next.tsx` | `#sport` |
| ۱۰ | روش سارا | `sara-method.tsx` | `#method` |
| ۱۱ | صحنه | `speaking.tsx` | `#speaking` |
| ۱۲ | محتوا | `media.tsx` | `#media` |
| ۱۳ | همکاری | `collaborate.tsx` | `#collaborate` |
| ۱۴ | درباره | `about-sara.tsx` | `#about` |
| ۱۵ | پایان | `final-cta.tsx` | `#start` |
| ۱۶ | فوتر | `contact.tsx` | `#contact` |

پوسته‌ی مشترک: `components/site/section-shell.tsx` — همان ریتم، همان عرض،
همان سرشناسه‌ی شماره‌دار. هر بخشی که این را نپیچد از ریتم می‌افتد.

### چهار انتخابِ ترتیبی که دلیل دارند

۱) **«درباره» آخر است، نه اول.** اولِ سایت بود، سایت می‌شد رزومه. بعد از
   داستان و شاهد، تبدیل می‌شود به جمع‌بندیِ شخصیتِ برند.
۲) **«تأثیر» بعد از داستان می‌آید** تا عدد، شاهدِ روایت باشد نه ادعای
   مستقل. اعدادش همان اعدادِ خودِ بابک از بخشِ روایتِ قبلی است؛ هیچ عددِ
   تازه‌ای ساخته نشد.
۳) **بازاریابی شبکه‌ای وسطِ زنجیره است**، نه اولش و نه آخرش، و در «مسیر»
   یکی از شش ایستگاه است. بی این، کلِ برند به یک صنعت تقلیل پیدا می‌کرد.
۴) **گالریِ نُه‌عکسی رفت داخلِ «صحنه»**. آن عکس‌ها خودشان عکسِ همایش‌اند،
   پس آن‌جا شاهدِ همان بخش‌اند؛ یک «گالری» بی‌عنوان در روایت جایی نداشت.
   فهرست از `lib/stage-images.ts` می‌آید (تنها منبعِ حقیقت) و
   `stage-sphere.tsx` بازنشسته شد.

### آشکارسازی با اسکرول — با CSS، نه با JS

`.reveal` و `.rule-in` در `globals.css` با `animation-timeline: view()`
کار می‌کنند و **داخل `@supports`**‌اند. دلیلش قاعده‌ی پروژه است: مرورگری
که این را ندارد هیچ انیمیشنی نمی‌گیرد و محتوا **دیده می‌شود**. با
IntersectionObserver اگر ناظر شلیک نکند محتوا نامرئی می‌مانَد — همان دامی
که در همین پروژه چند بار خوردیم، از جمله در نوارِ ناوبری.

سنجیده شد: در پنلِ مرورگرِ ابزار، انیمیشن درست وصل می‌شود
(`ViewTimeline`, `fill-mode: both`, `range: entry 5% cover 26%`) ولی
`currentTime` برابر `null` است، یعنی تایم‌لاین غیرفعال — و آن‌جا هم
شفافیتِ همه‌ی ۹۶ عنصر ۱ می‌مانَد. یعنی بدترین حالت، «بی‌انیمیشن ولی
خوانا» است.

### دو چیزی که عمداً پر نشد

**۰۶ — آدم‌ها.** هیچ توصیه‌نامه، اسم یا عکسی ساخته نشد. خودِ بابک شرط
گذاشته بود که این بخش فقط با نمونه‌های واقعیِ قابل‌اتکا پر شود. قابِ
آماده هست، سه کارت با برچسبِ صریحِ «نمونه‌ی ساختار»، و `PENDING = true`
در بالای فایل. برای انتشار، هر نمونه به رضایتِ کتبیِ همان شخص نیاز دارد.

**۰۹ — ورزش ۲.۰.** تصویرِ ورزشیِ سارا نرسیده؛ جای‌نگهدارِ برچسب‌دار
گذاشته شد و عکسِ نامربوط جایش نیامد. کلِ متنِ بخش هم زمانِ آینده دارد،
چون این «فصلِ بعد» است نه یکی از خدماتِ فعلی.

### اندازه‌گیریِ کنتراست — و دامِ خودِ اندازه‌گیری

هشت نقطه‌ی متنِ خاکستریِ کوچک زیر ۴.۵:۱ بودند (`neutral-500/600` روی
زمینه‌ی نزدیک به مشکی) و به `neutral-400` رفتند؛ سلسله‌مراتب از اندازه و
مونو و تراکینگ می‌آید نه از کم‌رنگی. دو مورد **برگشت** چون over-correction
بود: گزینه‌ی ردشده‌ی «منتظر بمانم» (در ۲۶px آستانه ۳:۱ است و ۴.۲۸ داشت)
و فلش‌های `aria-hidden`.

**دامِ اندازه‌گیری، دو بار:** تیلویند ۴ رنگ‌ها را `oklch()`/`oklab()`
می‌دهد و پارسرِ ساده‌ی regex آن‌ها را صفر می‌خواند — نسبت‌های ۱.۰ و ۱.۴۷
که اول درآمد، همه‌شان خطای ابزار بود نه سایت. و زمینه‌های نیم‌شفاف
(`bg-white/[0.02]`) اگر روی بومِ خالی نقاشی شوند **سفید** خوانده می‌شوند.
راهِ درست: رنگ را با canvas حل کن، و زنجیره‌ی زمینه‌ها را تا نخستین
لایه‌ی مات جمع کن و از پایین به بالا روی هم بگذار.

### سنجیده‌شده روی مرورگر

* ۱۶ بخش در `main` + فوتر؛ ارتفاع سند ۱۵۸۶۳px در ۱۴۴۰×۸۲۰.
* هیچ لنگرِ شکسته‌ای نیست (همه‌ی `href="#..."`ها هدف دارند).
* موبایل ۳۷۵px: سرریزِ افقی **صفر**، هیچ عنصری پهن‌تر از کادر نیست.
* هر هشت نقطه‌ی کنتراستِ اصلاح‌شده اکنون پاس.
* `next build` پاس، TypeScript بدون خطا، کنسول بدون خطا.

### تغییرِ کوچکِ هیرو که باید بدانی

خطِ نقش‌ها در هیروی ۳ از «کارآفرین · مربی · رهبر سازمان» شد
«کارآفرین · رهبر · مربی · ورزشکار» — به‌خاطرِ نکته‌ی جای‌گیریِ خودِ بابک
که برند نباید به نتورک تقلیل پیدا کند و «ورزشکار» بخشی از هویت است.
**ویدیو دست نخورد.** برای برگشت، فقط همان یک رشته در
`hero-v3-video.tsx` را عوض کن.

## بازطراحیِ بدنه — از «قالبِ پیش‌فرض» به روایتِ سینماتیک

بابک گفت ظاهرِ بدنه «زشت و خشک و هوش‌مصنوعی‌طور» است. حق داشت، و دو
اسکیل دلیلش را دقیق نام بردند:

`frontend-design` می‌گوید طراحیِ ساخته‌ی AI امروز حولِ سه قالب جمع شده و
یکی‌شان **عیناً** همان بود که ساخته بودم: «زمینه‌ی نزدیک به مشکی با یک
لهجه‌ی روشنِ تند». به‌علاوه هشدار می‌دهد که نشانگرهای شماره‌دار
(۰۱/۰۲/۰۳) وقتی محتوا **واقعاً** ترتیب ندارد، تزئین‌اند نه اطلاع — و من
هر شانزده بخش را شماره زده بودم.

`ui-ux-pro-max` هم الگوی درست را داد: **Scroll-Triggered Storytelling**
با نشانگرِ پیشرفت، و پالتِ «مشکیِ پریمیوم + لهجه‌ی گرم» به‌جای بنفش.

### پالت از داراییِ خودِ سارا، نه از سلیقه

با میانه‌ی پیکسل‌ها از فایل‌های خودش نمونه‌برداری شد:

| منبع | رنگ |
|---|---|
| بلوزِ سارا در گرافیک ABOUT ME | `#b6283f` |
| همان بلوز در نورِ خامِ عکس | `#d73448` |
| زمینه‌ی گرافیک | `#292629` |
| شالِ کرِم | `#dcd8d9` |
| دیوارِ هیروی ویدیویی | حدود `#d9cfc0` |

توکن‌های بدنه از همین‌ها آمد: `--color-rose #c8324a` (لهجه، همان بلوزِ
او)، `--color-rose-deep #9e1f33` (لهجه در اندازه‌ی متن روی کرِم)،
`--color-rose-lit #e06b7d` (لهجه روی تیره)، `--color-char #1a1719`،
`--color-bone #e4ddd3`، `--color-chalk #e6e1dc`، `--color-dust #a79e94`،
`--color-graphite #4a4239`. `--color-ink` هم از `#08070a` سرد به
`#14110f` گرم رفت.

**چرا این پالتِ پیش‌فرض نیست:** لهجه‌ی بدنه دقیقاً تنها چیزِ اشباعِ
کادرِ هیرو است — قرمزِ بلوزِ خودش. یعنی بدنه ادامه‌ی هیرو است، نه یک
دنیای جدا. و قرمزِ رُزیِ او با «تراکوتا روی کرِم» (قالبِ پیش‌فرضِ دیگر)
یکی نیست.

### قصه‌گویی با ارزشِ روشنایی — پرده، نه راه‌راه

سایت با روشنایی روایت می‌کند و یک‌درمیان نیست:

    هیرو (کرِم) → جمله‌ی برند (کرِم) → **داستان (تیره ← سقوط)**
    → مسیر، قلمروها (تیره) → **شاهد (کرِم ← نفسِ روشن)**
    → آدم‌ها … همکاری (تیره) → **درباره (کرِم)** → پایان (تیره) → فوتر

سه لحظه‌ی روشن میانِ تیره‌ها. تیره‌شدن در «داستان» خودش دراماتورژی است:
ارزش همان‌جا برمی‌گردد که سقوط اتفاق می‌افتد.

### امضا — تنها بخشِ پین‌شده

`site/choice-moment.tsx`. راهنمای موشن می‌گوید بیش از یک‌دو بخشِ
پین‌شده با حسِ اسکرولِ بومی می‌جنگد، پس **همه‌ی بولدیِ حرکتیِ پروژه در
همین یک نقطه** خرج شد و بقیه‌ی سایت آرام است. با اسکرول: انتخابِ ردشده
عقب می‌نشیند، خطِ قرمز از راست کشیده می‌شود، و «انتخاب کردم که عمل
کنم.» در ۹۶px بالا می‌آید. پارالاکس فقط روی هاله‌ی تزئینیِ پشت است —
هرگز روی متن، چون خواندن را سخت می‌کند.

### شماره‌ها: فقط جایی که ترتیب واقعی است

* **مانده** — مسیر (شش ایستگاهِ پشت‌سرهم)، روش (پنج گامِ ترتیب‌دار)،
  و سنِ مراحلِ داستان (۲/۱۶/۲۴).
* **رفته** — قلمروها، مربی‌گری، محتوا: این‌ها مجموعه‌اند نه توالی. و
  سرشناسه‌ی «۰۱ — LABEL» در همه‌ی بخش‌ها با یک برچسبِ فارسیِ کوتاه عوض
  شد.

### چیدمان‌هایی که از حالتِ کارت درآمدند

* **مسیر**: از گریدِ ۳×۲ به یک **خطِ ممتد با شش ایستگاه** (افقی در
  دسکتاپ، عمودی در موبایل). گرید ترتیب را گم می‌کرد و شش ایستگاهِ
  متفاوت را هم‌ارزش نشان می‌داد.
* **شاهد**: از چهار کارتِ هم‌عرض در قابِ درخشان به **عدد به‌عنوان تایپِ
  اصلی** روی کرِم (تا ۱۳۲px). و وزنشان یکسان نیست: «۱۲ سال» و «چندصد
  نفره» ادعای اصلی‌اند و بزرگ‌ترند.
* **نوارِ پیشرفت**: یک خطِ مویی زیرِ ناوبری با `animation-timeline:
  scroll()` — بی JS، داخل `@supports`.

### اندازه‌گیریِ کنتراست روی **همه‌ی** گره‌های متن

اسکریپت روی هر گرهِ متنیِ هر شانزده بخش اجرا شد (با حلِ رنگ در canvas و
compositing زنجیره‌ی زمینه‌ها):

* دورِ اول: **۲۶ خطا**.
* بخشِ بزرگی‌شان شماره‌های روحیِ `text-white/15` بودند (نسبت ۱.۶:۱). برای
  قلمروها/مربی‌گری/محتوا **حذف** شدند (تزئین بودند)؛ برای روش که ترتیبش
  واقعی است **خوانا** شدند.
* دورِ آخر: **صفر خطا**، بدون سرریزِ افقی.

### چیزی که در این محیط قابل بررسی نبود

پنلِ مرورگرِ ابزار بعد از هر اسکرول دیگر نقاشی نمی‌کند (فقط بلافاصله بعد
از `navigate` درست است) و هیرو `h-svh` است، پس **هیچ اسکرین‌شاتی از
بخش‌های تیره‌ی بدنه گرفته نشد**. ساختار، توکن‌ها، پرده‌ها، پین و کنتراست
همه عددی تأیید شدند، ولی قضاوتِ بصریِ نهایی باید در مرورگرِ خودِ بابک
انجام شود.

## کوتاه‌سازی — از ۱۱۵۰ کلمه به ۸۰۲

بابک گفت مطالب طولانی شده و بعضی بخش‌ها «تکرارِ مکررات به شکل‌های
مختلف» است. اندازه‌گیری تأییدش کرد؛ اسکریپت روی متنِ رندرشده‌ی هر بخش
اجرا شد و عبارت‌های ۳ تا ۶ کلمه‌ایِ مشترک را بیرون کشید:

| عبارت | در چند بخش |
|---|---|
| «بازاریابی شبکه‌ای» | ۴ |
| «توسعه‌ی فردی» | ۴ |
| «دوازده سال فعالیتِ حرفه‌ای در بازاریابی شبکه‌ای» | عیناً در ۲ |
| «یاد گرفتم، پیش از هر درسِ دیگری» | عیناً در ۲ |

### سه بخش از فلو بیرون رفت (فایل‌ها دست‌نخورده‌اند)

* **`FourWorlds`** — ۱۳۱ کلمه، دومین بخشِ بزرگِ سایت، و هر چهار قلمرواش
  جای دیگری هم بود: کسب‌وکار و رهبری در «مسیر» و «سازمان»، توسعه‌ی فردی
  در «مربی‌گری» و «روش»، ورزش در «داستان» و «فصل بعد».
* **`Media`** — سه دسته‌اش با سه مسیرِ «مربی‌گری» یکی بود، و محتوایی را
  وعده می‌داد که وجود ندارد (مقاله، پادکست، ویدیو).
* **`PeopleGrown`** — به بازدیدکننده «در انتظارِ محتوای واقعی» نشان
  می‌داد. آن پیام برای بابک است نه برای مخاطبِ سارا.

برگرداندن هرکدام: فقط `import` و تگش را در `page.tsx` بگذار.

### چهار تکرارِ متنی که بسته شد

۱. **درباره** ۱۰۹ → ۵۶ کلمه. کلِ داستان را دوباره تعریف می‌کرد. حالا
   جمع‌بندیِ شخصیت است، نه تکرارِ روایت.
۲. **سازمان** — «دوازده سال… چندصدنفره» حذف شد (در «شاهد» و «مسیر» هست)
   و دعوتش هم برداشته شد، چون دو بخش بعد «همکاری» همان دعوت را می‌کند.
۳. **مسیر** — اقلامِ «۱۲ سال فعالیت» و «سازمان چندصدنفره» از ایستگاه‌ها
   برداشته شد. جای عدد در «شاهد» است.
۴. **ستون‌های سازمان** — «رشدِ مالی» عیناً یکی از سه مسیرِ مربی‌گری بود؛
   ستون‌ها حالا درباره‌ی خودِ سازمان‌اند (ساختِ تیم، آموزشِ درون‌سازمانی،
   جانشین‌پروری، مقیاس‌پذیری).

**و یک تکرار که خودم تازه ساختم و گرفتم:** بیوگرافیِ نوِ «درباره» نوشته
بود «چیزی را آموزش نمی‌دهم که…» که عیناً جمله‌ی برند بود. عوض شد.

### وضعیت بعد از کوتاه‌سازی

* **۸۰۲ کلمه در ۱۳ بلوک** (از ۱۱۵۰ در ۱۶) — ۳۰٪ کوتاه‌تر.
* تکرارهای باقی‌مانده همه واژه‌ی مضمونی‌اند («توسعه‌ی فردی»، نامِ خودش،
  عنوان‌های سخنرانی) نه جمله‌ی تکراری.
* دعوت‌ها از پنج به چهارِ متمایز رسید: «شروعِ مسیرِ مربی‌گری»، «دعوت برای
  سخنرانی»، «همکاری با سارا»، «شروعِ مسیر با سارا».
* کنتراست: یک خطا پیدا و رفع شد — «منتظر بمانم» از پایه کم‌رنگ بود
  (۳.۹۲:۱). حالا در سکون خوانا است و کم‌رنگ‌شدنش کارِ تایم‌لاین است. این
  همان قاعده است: حالتِ سکون نباید به انیمیشن وابسته باشد.
* بدون سرریزِ افقی، بدون لنگرِ شکسته، `next build` پاس.

## خط تیره و فوترِ یگانه

### خط‌های تیره

بابک گفت همه‌ی خط‌تیره‌ها از متن‌ها برداشته شود. اسکریپت اول کامنت‌های
کد را جدا کرد تا فقط کپیِ قابل‌دیدن دست بخورد: **۱۴ مورد** پیدا شد و
هرکدام **بازنویسی** شد، نه حذفِ خشک — چون حذفِ ساده جمله را می‌شکند:

| پیش | پس |
|---|---|
| «نه ادعا — شاهد» | «نه ادعا، شاهد» |
| «…یاد گرفتم — پیش از هر درسِ دیگری» | «…یاد گرفتم، پیش از هر درسِ دیگری» |
| «فهمیدم آنچه… — تمرین، تکرار، مسئولیتِ نتیجه — بیرون از زمین…» | «فهمیدم تمرین، تکرار و مسئولیتِ نتیجه بیرون از زمین هم کار می‌کند» |
| «موضوع‌ها — KEYNOTE TOPICS» | «موضوع‌ها» |
| «© ۱۴۰۵ — تمام حقوق محفوظ است» | «© ۱۴۰۵، تمام حقوق محفوظ است» |

سنجیده شد: **صفر** خط‌تیره در متنِ قابل‌دیدنِ کلِ سایت. کامنت‌های کد
دست‌نخورده‌اند؛ آن‌ها متنِ سایت نیستند.

### یک فوتر به‌جای دو

سایت دو پایان داشت: `final-cta.tsx` (ساخته‌ی من) و `contact.tsx` (که
ویدیوی بابک را داشت). به‌خواستِ او **اولی ماند** و ویدیو به آن منتقل شد.
برای این‌که چیزی از دست نرود، آیکون‌های شبکه‌های اجتماعی و خطِ کپی‌رایت و
اعتبارِ AFA.CO هم منتقل شدند و `contact.tsx` از فلو بیرون رفت (فایلش
هست).

`id="contact"` روی همین فوتر نشست، چون بیشترِ لنگرهای سایت به آن اشاره
می‌کنند؛ دعوتِ نوار هم از `#start` به `#contact` رفت.

**قرص‌ها `mailto` شدند.** وقتی فوتر و بخشِ کنش یکی شد، لینکِ قرص‌ها به
`#contact` یعنی لینک به خودشان. حالا سه‌تای اول `mailto` با موضوعِ آماده
دارند و چهارمی به بالای روایت برمی‌گردد.

### دامِ متن روی ویدیو — با عدد گرفته شد

روشن‌ترین نقطه‌ی فریمِ ویدیوی فوتر روشناییِ **۰.۶۵۹** دارد. با شفافیتِ
۵۰٪ روی زمینه‌ی تیره و پرده‌ی اولیه، زمینه‌ی مؤثر ۰.۱۳۲ می‌شد و متنِ
کوچکِ خاکستری آن‌جا فقط **۲.۱۸:۱** می‌گرفت — یعنی ناخوانا روی بخشی از
لوپ.

دو اصلاح: پرده از ۰.۶۲ به ۰.۷۴ عمیق‌تر شد (زمینه‌ی مؤثر ۰.۰۹۴) و متنِ
کوچک از خاکستری به روشن رفت. حالا تیتر **۵.۶۳:۱** و متنِ کوچک
**۴.۹۴:۱** — هر دو بالای آستانه، **در بدترین فریمِ ویدیو**.

این همان دامی است که در نوارِ ناوبریِ روی هیرو هم خوردیم: روی ویدیو،
کنتراست را باید با فریمِ واقعی سنجید نه با رنگِ زمینه.

## اولویت‌های سارا، تاریِ ویدیو، و زمینه‌ی قرمز

### اولویت‌ها — ورزش فقط یک‌جا

سارا گفت اولویتش با **انتخابِ عمل‌کردن، توسعه‌ی فردی، ورود به کسب‌وکار،
بازاریابی شبکه‌ای و رهبریِ سازمان** است و ورزش اولویتِ بعدی است که یک
اشاره کافی است. چهار تغییر:

* `SportNext` («فصل بعد») از فلو بیرون رفت. فایلش هست.
* «مسیر» از شش ایستگاه به **پنج** رسید و ایستگاهِ جداگانه‌ی ورزش
  برداشته شد. پنج ایستگاهِ نو دقیقاً همان اولویت‌هایند: تحصیل و توسعه‌ی
  فردی · ورود به کسب‌وکار · بازاریابی شبکه‌ای · رهبری سازمان · مربی‌گری.
* «جمله‌ی برند» با ورزش شروع می‌شد؛ حالا با توسعه‌ی فردی و کسب‌وکار
  شروع می‌شود.
* «ورزشکار» از فهرست نقش‌های «درباره» و «پروژه‌های ورزشی» از فهرستِ
  «همکاری» برداشته شد.

**ورزش کجا مانده:** «داستان» (اشاره‌ی اصلی و لازم، ریشه‌ی انضباط)، نامِ
مدرکش در «مسیر» (علوم ورزشی / مدیریت ورزشی — واقعیت است)، و خطِ نقش و
جمله‌ی هیرو که **متنِ خودِ بابک** است و دست نخورد.

### تاری ویدیو — علت پیدا شد و رفع شد

بابک گفت چهره تار است. اندازه‌گیری: بیشینه‌ی شارپیِ منبع **۱۱۸** و خروجی
**۷۳**. علت افکت نبود، سه چیز بود که در `edit2.py` جمع شده بودند:

۱. **میانگین‌گیریِ فریم.** برای هر فریمِ خروجی دو فریمِ منبع وزنی جمع
   می‌شد. با ۲۴→۳۰fps تقریباً همه‌ی فریم‌ها کسری می‌افتند، پس **کلِ
   ویدیو** شبح‌دار می‌شد نه فقط اسلوموشن. حالا نزدیک‌ترین فریم برداشته
   می‌شود (تکرارِ فریم، روشِ استاندارد).
۲. **واسطه‌ی JPEG** با `-q:v 2` → حالا PNG.
۳. **CRF 20** → ۱۷.

به‌علاوه بلرِ ورودی از **۲۲px تا ۱.۹s** به **۱۰px تا ۱.۱s** کم شد. آن
۱.۹ ثانیه ۲۴٪ از لوپ بود و همان چیزی است که به چشمش آمده بود. یک
unsharp ملایم هم برای جبرانِ بزرگ‌نماییِ مرورگر اضافه شد.

نتیجه: شارپی **۷۳ → ۳۱۲** (بخشی از این عدد از unsharp است، ولی چشمی هم
تأیید شد: چشم، خط چشم، دندان و تارِ مو خوانا، بدون هاله).

**سقفِ رزولوشن:** منبع ۱۹۲۰×۱۰۸۰ است و کادرِ تأییدشده ۱۲۰۲×۶۷۶ از آن
برش می‌خورد، پس بیشتر از این پیکسل وجود ندارد. (`555.mp4` که ۲۸۷۸×۱۷۲۲
است، ضبطِ صفحه‌ی ellieswift.com است نه کلیپِ سارا.)

### زمینه‌ی قرمز — تست، قابل برگشت

سارا گفت رنگ‌های تیره و گرم را دوست دارد. زمینه‌ی پرتره‌ی استودیویی‌اش
اندازه‌گیری شد: میانه **#53040e** (چارک‌ها #4c030c تا #590610) — یعنی
همان خانواده‌ی `--color-crimson #4a020c` که از قبل در پروژه بود.

`build-assets/edit4-red.py`: ماتِ فریم‌به‌فریم با rembg، آستانه و
بزرگ‌ترین ناحیه‌ی پیوسته، یک پیکسل تو‌دادنِ لبه (تا هاله‌ی کرِمِ دیوارِ
قبلی دورِ مو نماند)، و ترکیب روی گرادیانِ رادیالی که همان پروفایلِ
پرتره را دارد.

**تغییرهای وابسته که لازم شد:** متنِ هیرو از مرکبِ تیره به روشن رفت،
پرده از کرِم به قرمزِ تیره، و `navTone` هیروی ۳ در رجیستری از `onLight`
به `onDark`. بی این‌ها متن روی قرمز ناخوانا می‌شد.

کنتراست روی روشن‌ترین نقطه‌ی فریمِ قرمز: نوار **۱۳.۰۲:۱**، نام
**۱۲.۱:۱**، خطِ نقش **۸.۷۷:۱**، جمله **۱۰.۹۹:۱**.

**برگرداندن:** فایل‌های کرِم (`sara-hero.mp4/webm/-poster.jpg`) سرِ
جایشان هستند. کافی است در `hero-v3-video.tsx` سه مسیر به نسخه‌ی بی‌`-red`
برگردد، رنگ‌های متن و پرده به حالتِ کرِم، و `navTone` به `onLight`.

### ایرادِ زمینه‌ی قرمز که از اسکرین‌ریکوردر پیدا شد

بابک ضبطِ صفحه فرستاد. سه فرضیه آزموده شد و **دوتایش رد شد** — ثبت
می‌شوند تا دوباره دنبالشان نرویم:

| فرضیه | اندازه‌گیری | نتیجه |
|---|---|---|
| هاله‌ی کرِم دورِ مو | گذارِ زمینه‌به‌شال فقط **۲ پیکسل** | رد |
| سارا شناور است | سوژه تا **۹۹.۹٪** ارتفاع می‌رسد، پوششِ سطرِ آخر ۳۰–۴۱٪ | رد |
| لبه‌ی مات «می‌جوشد» | ماتِ خام در مکث فقط **۰.۲۱٪** سطح جابه‌جا می‌شود (~۰.۱۵px) | رد |

**ایرادِ واقعی:** سارا در فریمِ ۱۶۳ منبع (**۶.۷۹ ثانیه**) کامل از کادر
بیرون می‌رود، ولی منبع ۱.۲۱ ثانیه فریمِ خالی بعدش دارد و نسخه‌ی اول
همه‌اش را برمی‌داشت. یعنی **۱۴٪ از لوپ، کادرِ قرمزِ خالی** بود. روی
دیوارِ کرِم آن سکوت طبیعی به‌نظر می‌رسید؛ روی قرمزِ تیره مثل خرابیِ ویدیو
دیده می‌شود.

آخرین بخش از ۷.۹۴ به **۷.۰۰ ثانیه** آمد. لوپ ۹.۳۰ → **۸.۳۰ ثانیه** و
کادرِ خالی ۱.۳ → **۰.۳ ثانیه (۴٪)**، فقط به‌اندازه‌ی بستنِ درزِ لوپ.

**دامِ اندازه‌گیری:** اولین سنجشِ «جوشش» را روی *خروجیِ فشرده* انجام
دادم و ۱۲۴۶ پیکسل درآمد؛ میانه‌ی زمانی گذاشتم و هیچ فرقی نکرد (۱۲۶۳).
بعد فهمیدم آن عدد نویزِ H.264 روی لبه بود نه ماتِ ناپایدار. سنجشِ درست
روی **خودِ آلفا** است، نه روی ویدیوی انکودشده. میانه برداشته شد.

**نکته‌ی محیط:** در پنلِ مرورگرِ ابزار `video.play()` بی‌خطا برمی‌گردد
ولی `currentTime` جلو نمی‌رود — همان محدودیتی که rAF و
`animation-timeline` را هم نگه می‌دارد. در ضبطِ خودِ بابک ویدیو پخش
می‌شود، پس عیبِ کد نیست.

### هیرو از ویدیوی قرمزِ خودِ بابک (نسخه‌ی جاری)

بابک خودش کلیپ را با زمینه‌ی قرمز رندر کرد
(`ax sara/Sara_adjusting_scarf_..._20260910131928.mp4`، ۱۹۲۰×۱۰۸۰، ۸s،
۲۴fps). پس **هیچ ماتی در کار نیست** — نه rembg، نه ترکیب. خط لوله همان
`edit3.py` است و فقط نقشه‌ی زمانی عوض شد.
اسکریپت: `build-assets/edit6-his-red.py`.

**چرا زمان‌بندی عوض شد.** کلیپِ او ریتمِ دیگری دارد. از خودِ فایل
اندازه‌گیری شد (سطحِ سوژه، مرکزِ افقی، شارپی و حرکت، هر ۱/۸ ثانیه):

| بازه | چه اتفاقی می‌افتد |
|---|---|
| ۰.۰۰–۰.۸۸ | کادر خالی |
| ۰.۹۰–۱.۹۵ | ورود از راست، مرکز ۹۴٪ → ۶۸٪ |
| ۱.۹۵–۳.۴۰ | نشستن |
| ۳.۴۰–۵.۴۰ | **مکث و لبخند** — بیشینه‌ی شارپی (۸۷–۱۰۵)، کمینه‌ی حرکت |
| ۵.۴۰–۶.۴۵ | جمع‌وجور شدن |
| ۶.۴۵–۷.۵۵ | خروج به چپ |

مرکزِ او در مکث روی **۶۵.۵٪** عرضِ منبع می‌افتد — همان عددِ نسخه‌ی کرِم —
پس همان برشِ ۱۲۰۲×۶۷۶ از لبه‌ی راست کار می‌کند و سارا در ۴۴.۹٪ کادر
(نیمه‌ی چپ) می‌نشیند.

**چرا در ۷.۵۵ تمام می‌شود.** در کادرِ کاملِ ۱۹۲۰ او تا انتهای کلیپ هنوز
کمی پیداست، ولی برش از ۳۷.۴٪ به بعد است و لبه‌ی راستش در **۷.۳۵** از آن
رد می‌شود. پس ۰.۲ ثانیه کادرِ خالی برای بستنِ درزِ لوپ کافی است.
خروجی: **۷.۹۰ ثانیه، ۵٪ کادرِ خالی**.

**رنگ.** قرمزِ او **#66191b** است — روشن‌تر و گرم‌تر از قرمزِ پرتره‌ی
استودیویی (#53040e) که توکن‌های `crimson` از آن آمده‌اند. پس رنگِ بخش
(`#4d1214`) و پرده (`rgba(38,9,10,…)`) با قرمزِ او هماهنگ شد، نه با
توکن‌ها؛ وگرنه ته‌رنگ می‌پرید.

**دامِ کنتراست که با همین تغییرِ رنگ برگشت.** قرمزِ روشن‌تر یعنی حاشیه‌ی
کم‌تر، و سارا هنگام ورود و خروج از پشتِ ستونِ متن رد می‌شود: روشناییِ
پشتِ متن به **۰.۳۲** می‌رسید. با پرده‌ی قبلی (۰.۶) خطِ نقش **۳.۶۹:۱** و
جمله **۴.۴۵:۱** می‌شد — هر دو زیر آستانه. پرده به **۰.۷۸** رفت و متن هم
روشن‌تر شد (نقش ۷۰٪ → ۸۵٪، جمله ۹۰٪ → ۱۰۰٪).

نتیجه‌ی سنجش روی روشن‌ترین فریم: نوار **۷.۳۳:۱** · نام **۹.۶۱:۱** ·
خط نقش **۸.۳۲:۱** · جمله **۹.۶۱:۱**.

**برگشت به کرِم:** `sara-hero.*` دست‌نخورده است. سه مسیر در
`hero-v3-video.tsx`، رنگ‌های بخش و پرده و متن، و `navTone` در رجیستری.

## `edit7-sharp-slow.py` — ساخته شد، بابک پس زد

تاریِ ورود دو منبع داشت و هر دو اندازه‌گیری شد: فیلترِ گاوسیِ ۱۰ پیکسلیِ
خودمان تا ثانیه‌ی ۱.۱۰، و تاریِ حرکت در خودِ کلیپ (شارپیِ سر ۷ تا ۱۳ در
ورود، در برابر ۱۰۰ در مکث). `edit7` اولی را برداشت و دومی را با
واهم‌آمیزیِ وینر (هسته‌ی افقی L=۱۱، nsr=۰.۰۲۰، فقط روی سوژه، با شدتِ
وابسته به شارپیِ همان فریم) درمان کرد، و سرعت را پله‌ای کرد تا اسلوموشن
روی «رسیدن» بیفتد نه روی تارترین بخشِ ورود.

عددها بهتر شدند (شارپیِ ثانیه‌ی اول ۳.۷ → ۱۹ تا ۳۷) ولی **بابک نتیجه را
بدتر دید و خواست به قبل برگردد.** فایل‌ها از `build-assets/prev-red/`
برگردانده شدند و بایت‌به‌بایت با نسخه‌ی `edit6` یکی‌اند؛ توضیحِ کامپوننت
هم برگشت.

**درسی که از این می‌مانَد:** شارپیِ اندازه‌گیری‌شده معیارِ خوبی برای «تار
هست یا نه» است، ولی معیارِ «قشنگ هست یا نه» نیست. واهم‌آمیزی جزئیاتِ
واقعی برمی‌گرداند و همزمان بافتِ پوست را سخت و پردازش‌شده می‌کند؛ آن را
عدد نشان نمی‌دهد. اگر دوباره سراغش رفتی، اول یک کلیپِ کوتاه برای بابک
بفرست، نه اسکرین‌شات — قضاوتِ چهره در حرکت با قضاوتِ فریمِ ثابت فرق دارد.

`edit7-sharp-slow.py` نگه داشته شد تا اگر لازم شد بشود بخشی از آن (مثلاً
فقط ورودِ آهسته، بی هیچ کارِ تصویری) دوباره اجرا شود.

## «درباره» آمد بالا — بین جمله‌ی برند و داستان

بابک اسکرین‌شاتِ سایتِ شخصِ دیگری را فرستاد (پرتره در یک سو، معرفیِ
کوتاه در سوی دیگر، و یک جدولِ فشرده‌ی واقعیت‌ها زیرش) و خواست چنین چیزی
بینِ جمله‌ی برند و داستان باشد.

**دو «درباره» ساخته نشد؛ همان یکی جابه‌جا شد.** بخشِ پایینیِ صفحه همین
است که آمد بالا. دو معرفیِ پرتره‌دار در یک صفحه، دقیقاً همان تکرارِ
مکرراتی می‌شد که بابک قبلاً از آن گله داشت.

استدلالِ قبلی («About در بالا سایت را رزومه می‌کند») نصفش درست بود: چیزی
که سایت را رزومه می‌کند *جای* معرفی نیست، *جنسِ* آن است. پس جا عوض شد و
جنس هم عوض شد.

### تقسیمِ کار بین سه بخشِ پشتِ سرِ هم

هر جمله باید چیزی بگوید که بالا و پایینش نگفته‌اند:

| بخش | چه می‌گوید |
|---|---|
| جمله‌ی برند (بالا) | *چه* کرده‌ام |
| **درباره (تازه)** | *با چه کسانی* کار می‌کنم و *با چه معیاری* |
| داستان (پایین) | *کِی* و از کجا |

به همین دلیل فهرستِ «توسعه‌ی فردی، کسب‌وکار، بازاریابی شبکه‌ای، رهبری»
در «درباره» تکرار نشد — عیناً دو پاراگراف بالاتر است. از متنِ قدیمیِ
«درباره» دو جمله‌ی خوبش ماند (معیارِ «اول خودم زیرِ فشارش ایستاده‌ام» و
«امروز وقتم بین… تقسیم می‌شود») و بقیه با یک پاراگرافِ تازه عوض شد که
مخاطبِ سارا را معرفی می‌کند.

### جدولِ چهارخانه‌ای

ایده از همان اسکرین‌شات، ولی **هیچ عددِ تازه‌ای ساخته نشد**: نقش، سابقه
(۱۲ سال)، زمینه، تمرکز — هر چهارتا از چیزی می‌آید که سارا پیش‌تر داده
است. عددهای «تأثیر» (۵۰۰ نفر، ۱۵٬۰۰۰ دلار) عمداً این‌جا نیامدند تا وقتی
به آن بخش می‌رسیم تازه بمانند.

### عکس

`portrait-lobby-900.jpg` انتخاب شد نه پرتره‌ی استودیوییِ قرمز. هیرو خودش
قرمزِ استودیویی است و تکرارِ همان نور و همان زمینه، دو بخش را شبیهِ یک
عکسِ دوباره‌چاپ‌شده می‌کند. فایل دقیقاً ۳:۴ است و قاب هم ۳:۴، پس برشی رخ
نمی‌دهد؛ `object-[55%_center]` فقط برای عرض‌های کوچک است — سوژه بین
۲۹.۷٪ تا ۷۹.۸٪ عرضِ فایل است، یعنی مرکزش ۵۵٪ نه ۵۰٪ (اندازه‌گیری‌شده).

### دو چیز که سنجش گرفت

- **تضادِ برچسبِ جدول.** `text-graphite/70` در ۱۱ پیکسل **۳.۶۱:۱** می‌داد،
  زیرِ آستانه. بی `/70` شد ۷.۳۲. برچسب‌بودنِ آن کلمه از اندازه و فاصله‌ی
  حروف می‌آید، نه از کم‌رنگ‌کردنش. بعد از اصلاح، هیچ عنصری در این بخش زیر
  آستانه نیست.
- **ترتیب در موبایل.** با چیدنِ ستون‌ها روی هم، عکس پشتِ سه پاراگراف و
  جدول می‌افتاد: چهره‌ی سارا بعد از حدود ۷۰۰ پیکسل متن. `order-first` +
  `lg:order-none` آن را در موبایل اول می‌آورد و در دسکتاپ سرِ جایش
  (متن راست، عکس چپ — در ۱۲۸۰ اندازه گرفته شد: متن ۶۰۲ تا ۱۱۸۹، عکس ۷۷
  تا ۵۳۸). سرریزِ افقی در ۳۷۵ و ۱۲۸۰ صفر.

**عکس در پنل مرورگر رسم نشد** — همان دامِ شماره‌ی ۱۰. خودِ تصویر سالم است
و با خواندنِ پیکسل تأیید شد: decode شده، ۹۰۰×۱۲۰۰، میانگینِ رنگ
(۱۶۶،۱۳۲،۹۵) یعنی همان طلاییِ لابی، و ۴۰۲۹ رنگِ متمایز در یک نمونه‌ی
۶۰×۸۰. قابِ خالی در اسکرین‌شات ایرادِ کد نیست.

## «داستان» و «مسیر حرفه‌ای» — دو بازطراحی

### داستان: کارت‌های پراکنده روی خطِ چین‌دار

بابک کامپوننتِ «How it works» را از 21st.dev فرستاد. سازوکارش خوب است و
ماند: کارت‌ها روی یک مسیرِ مارپیچ پخش می‌شوند و خطِ چین‌دار بینشان آرام
جلو می‌رود. **سطحش تماماً عوض شد**، چون همان چیزی بود که کلِ پروژه صرفِ
پاک‌کردنش شده:

| در کامپوننتِ اصلی | این‌جا |
|---|---|
| Comic Sans برای شماره | پیدا، با رقمِ فارسی |
| نارنجی/آبی/بنفشِ پاستلی، کارتِ سفید، سایه‌ی خاکستری | `char-2` و رُز |
| سنجاقِ کاغذ، استعاره‌ی کاغذِ یادداشت | حذف |
| `left-[15%]`/`right-[15%]` فقط LTR | آینه؛ مسیر از راست شروع می‌شود |
| `m.path` + rAF | انیمیشنِ CSS، بخش سرور می‌مانَد |
| چرخشِ ۸ درجه | ۱.۶ درجه |

سنجاقِ کاغذ و کارتِ رنگیِ سنجاق‌شده برای روایتِ ورشکستگیِ خانواده کوچک
بود. چرخشِ هشت درجه «تخته‌ی اعلاناتِ مدرسه» است؛ یک‌ونیم درجه فقط می‌گوید
دستی چیده شده.

**«سقوط» کارتِ چهارم شد.** پیش‌تر یک بلوکِ جدا زیرِ فهرست بود. حالا روی
همان مسیر است ولی از الگو بیرون می‌زند: سن ندارد، قابش رُز است، و خطی که
به آن می‌رسد کم‌رنگ و رُز می‌شود. *شکسته‌شدنِ خودِ مسیر* حرفِ این بخش است؛
پیش‌تر این را فقط با متن می‌گفتیم.

**دامِ هندسی که خوردم.** نسخه‌ی اول: خط اصلاً دیده نمی‌شد. علت با
اندازه‌گیری پیدا شد — کارت‌ها ۳۲۰ پیکسل بلند بودند و فاصله‌ی عمودیشان
۲۳۲، یعنی **۶۱ پیکسل روی هم می‌افتادند** و هیچ دهانه‌ای نمی‌ماند که خط در
آن دیده شود. فاصله به ۴۰۰ رفت (۳۲۰ + ۸۰ دهانه) و ارتفاعِ ظرف به ۱۴۶۰.

دو قیدِ دیگر که از اندازه‌گیری آمد:

- **`viewBox` و ظرف یک دستگاه‌اند.** ارتفاعِ `viewBox` دقیقاً برابرِ
  ارتفاعِ ظرف است و `preserveAspectRatio="none"`، پس واحدِ عمودی
  یک‌به‌یک پیکسل است. مرکزِ افقیِ کارت‌ها **اندازه‌گیری شد** (۷۸۰ و ۲۲۰)
  و همان عددها در مسیر رفت؛ حدسِ اولیه‌ام ۷۴۰/۲۶۰ بود و ۱۰ واحد خطا
  داشت.
- **کششِ نقاطِ کنترل ۱۲۰ است نه ۷۰.** فاصله‌ی افقی (۵۶۰ واحد) خیلی
  بیشتر از عمودی (۱۸۰) است، پس با ۷۰ منحنی عملاً یک خطِ مورب می‌شد.
- **چیدمانِ مطلق از `lg` شروع می‌شود نه `md`.** در ۷۶۸ پیکسل دو کارتِ
  ۲۸۰ پیکسلی تقریباً به هم می‌چسبند و باز دهانه‌ای نمی‌مانَد.

عددهای `lg:top-*` و `lg:h-*` باید با `TOPS` و `HEIGHT` یکی بمانند و
به‌صورتِ رشته‌ی کامل نوشته شده‌اند: Tailwind v4 متنِ سورس را اسکن می‌کند و
کلاسِ ساخته‌شده در زمانِ اجرا را نمی‌بیند.

### مسیر حرفه‌ای: تیترهای درشت و قابِ عکس

چیدمان از سایتِ تونی رابینز که بابک فرستاد. آینه شد برای فارسی: تیترها
سمتِ **راست** (جایی که خواندن شروع می‌شود) و عکس سمتِ **چپ** — همان چیزی
که بابک خواست. در ۱۲۸۰ اندازه گرفته شد: تیترها ۶۳۸ تا ۱۱۸۹، عکس ۷۷ تا ۵۷۴.

**چرا تعاملی است و نه فقط تزئینی.** نسخه‌ی مرجع یک عکسِ ثابت دارد. این‌جا
پنج ایستگاه پنج عکس دارند، پس تعامل کارِ واقعی می‌کند: با رفتن روی هر
ایستگاه هم عکسش می‌آید و هم اقلامِ زیرمجموعه‌اش باز می‌شود. بی این،
تیترهای درشت جا برای اقلام نمی‌گذاشتند و فهرست به همان کارت‌های قبلی
برمی‌گشت.

چون این «افشای اطلاعات» است و نه فقط عوض‌شدنِ عکس، هر ایستگاه دکمه‌ی
واقعی است با `aria-expanded` — نه یک `li` که فقط `hover` بفهمد. با
صفحه‌کلید و روی لمسی هم باز می‌شود. اقلام `min-h` دارند تا باز و بسته
شدنشان ستون را بالا و پایین نپراند. ایستگاهِ اول از ابتدا باز است، پس
حالتِ سکون بی هیچ تعاملی کامل است.

عکس‌ها `aria-hidden` و `alt=""`اند: هر پنج‌تا همزمان در DOMاند و اگر متنِ
جایگزین داشتند خواننده‌ی صفحه هر پنج توضیح را پشتِ سرِ هم می‌خواند در
حالی که فقط یکی دیده می‌شود.

**نقطه‌ی کانونیِ هر عکس با rembg اندازه‌گیری شد** و با همان رابطه‌ی
`stage-images.ts` برای کادرِ **۴:۵** حساب شد:

| ایستگاه | فایل | object-position |
|---|---|---|
| تحصیل و توسعه‌ی فردی | `mastermind-hall-1900` | `25% 50%` |
| ورود به کسب‌وکار | `flags-corridor-1400` | `62% 50%` |
| بازاریابی شبکه‌ای | `pmm-anniversary-1400` | `49% 50%` |
| رهبری سازمان | `pmm-first-leadership-900` | `50% 0%` |
| مربی‌گری | `roundtable-laugh-1100` | `20% 50%` |

قاب در همه‌ی بریک‌پوینت‌ها **۴:۵ می‌مانَد** و کشسان نیست: همین عددها برای
نسبت‌های ۰.۸ و ۱.۰ و ۱.۲۵ حساب شد و هر کدام تا ۲۰ واحد جابه‌جا می‌شدند.
هیچ‌کدام از این پنج عکس جای دیگری در سایت استفاده نشده، پس با گالریِ
«صحنه» تکرار نمی‌شوند.

`panel-seated-1200.jpg` عمداً استفاده **نشد**: نفر دومِ قابل‌تشخیص دارد و
قاعده‌ی رضایت هنوز پابرجاست.

### سنجش

تضاد در هر دو بخش صفر شکست. تیترِ فعال ۱۳.۷:۱، تیترهای خاموش ۳.۷۳:۱ در
۴۳.۵ پیکسل (آستانه‌ی متنِ درشت ۳:۱) — `text-chalk/60` تفاوتِ دیداریِ
محسوسی نمی‌ساخت، `/45` می‌سازد و هنوز می‌گذرد. سرریزِ افقی در ۳۷۵ و ۱۲۸۰
صفر؛ در موبایل کارت‌های داستان ستونی می‌شوند و خط پنهان، و در «مسیر» عکس
اول می‌آید. `tsc --noEmit` و `next build` تمیز.

**پنلِ مرورگر باز هم عکس‌ها را بعد از تعویضِ کلاس کامپوزیت نکرد** و
`getComputedStyle` هم مقدارِ ترنزیشن را به‌روز نکرد. درستیِ منطق با خواندنِ
`className` تأیید شد (کلاسِ `opacity-100` درست جابه‌جا می‌شود). دامِ
شماره‌ی ۱۰ و ۱۱، نه ایرادِ کد.

## سه بخش برداشته شد، و بلرِ ورودِ هیرو رفت

### چه چیزی از فلو بیرون رفت

| بخش | بود | حالا |
|---|---|---|
| `ChoiceMoment` — «لحظه‌ی انتخاب» | داخلِ «داستان»، تنها بخشِ **پین‌شده**ی سایت | برداشته شد؛ حرفش در یک بند ماند |
| `Impact` — «شاهد» | بعد از «مسیر» | برداشته شد |
| `NetworkMarketing` — «سازمان» | بعد از «مربی‌گری» | برداشته شد |

هر سه فایل دست‌نخورده‌اند. برای برگرداندن فقط `import` و تگ لازم است.

**«لحظه‌ی انتخاب» چه شد.** بابک خواست از «از این‌جا دو انتخاب داشتم» تا
«دوازده سالِ بعدم را ساخت» پاک شود، ولی همزمان خواست به خودِ *انتخاب*
اشاره شود. پس بخش رفت و این بند جایش نشست، ته‌ی «داستان»:

> انتخاب کردم عامل باشم. به جای انتظار برای کسی که وضعیت را درست کند،
> دوازده سالِ بعدی را خودم ساختم.

آن بخش یک صفحه‌ی کامل اسکرولِ پین‌شده می‌گرفت تا همین را بگوید. با رفتنش،
**سایت دیگر هیچ بخشِ پین‌شده‌ای ندارد** و GSAP/ScrollTrigger از صفحه‌ی
زنده بیرون است (کتابخانه هنوز نصب است و `choice-moment.tsx` تنها
مصرف‌کننده‌اش بود).

**عددهایی که با «شاهد» رفتند.** ۵۰۰ نفر و ۱۵٬۰۰۰+ دلار دیگر **هیچ‌جای
سایت نیستند**. «۱۲ سال» هنوز در جدولِ «درباره» هست. اگر سارا بخواهد آن دو
عدد بمانند، جای طبیعی‌شان همان جدول است.

**نوار ناوبری.** لینکِ «تأثیر» به `#impact` می‌رفت که دیگر وجود ندارد و
کاربر را به ته صفحه می‌پراند. جایش «روش» → `#method` نشست. سنجیده شد:
هیچ لنگرِ مرده‌ای در کلِ صفحه نمانده.

### هیرو: فقط بلر رفت، نه بیشتر

`edit7` سه کار با هم کرد (حذفِ بلر، واهم‌آمیزیِ وینر، سرعتِ پله‌ای) و بابک
همه‌اش را پس زد. این بار فقط **یک** عدد عوض شد:

    BLUR_MAX: 10.0 → 0        (`edit8-noblur.py`)

هیچ ترمیمِ تصویریِ تازه‌ای اضافه نشد — همان چیزی که بارِ قبل پوست را سخت و
پردازش‌شده نشان داد. زمان‌بندی، آنشارپ، برش و CRF دست‌نخورده. فریم‌های
ورود حالا به‌جای بلر، همان آنشارپِ معمولِ کلیپ را می‌گیرند.

سنجشِ شارپیِ سر، قبل و بعد:

| ثانیه | با بلر | بی بلر |
|---|---|---|
| ۰.۱۷ | ۴ | **۱۰** |
| ۰.۵۰ | ۵ | **۱۱** |
| ۰.۸۳ | ۱۳ | **۲۰** |
| ۱.۰۰ | ۲۱ | **۲۶** |
| ۱.۳۳ | ۹۱ | ۸۷ |
| ۱.۵۰ | ۹۵ | ۹۶ |
| ۲.۱۷ | ۷۸ | ۷۶ |

از ۱.۳۳ به بعد دو نسخه یکی‌اند — یعنی واقعاً فقط بلر برداشته شده و چیزِ
دیگری دست نخورده. مدت همان ۷.۸۷ ثانیه، mp4 ۱۷۶۴KB، webm ۵۶۸KB.

**نرمیِ باقی‌مانده‌ی ثانیه‌ی اول** تاریِ حرکت در خودِ فایلِ بابک است (سارا
۳۰ تا ۳۸ پیکسل در هر فریم جابه‌جا می‌شود) و با هیچ فیلترِ بی‌خطری
برنمی‌گردد. تنها راهش همان واهم‌آمیزی بود که رد شد. نسخه‌ی با بلر هنوز در
`build-assets/prev-red/` است.

## شش اصلاح: جمله‌ی موفقیت، متنِ درباره، کارتِ انتخاب، و درزِ هیرو

### ۱. جمله‌ی «موفقیت» زیر هیرو

«در واقع موفقیتِ من فقط چیزی نیست که به دست آورده‌ام، چیزی است که در
دیگران ایجاد کرده‌ام.» این جمله پیش‌تر در بخشِ «شاهد» بود و با حذفِ آن بخش
از سایت رفته بود. حالا در `brand-statement.tsx` است، بعد از بندِ فهرست.
اندازه‌اش (۳۲px) عمداً بینِ تیترِ بخش (۵۸px) و متنِ عادی (۱۷.۵px) است تا
ریتمِ بلند-کوتاه-متوسط بسازد و با تیتر رقابت نکند. تضاد ۱۲.۸:۱.

### ۲. متنِ «درباره» از خودِ سارا

سه بندِ قبلی که من نوشته بودم جای خود را به **ترجمه‌ی پاراگرافی داد که
خودِ سارا برای کارتِ «ABOUT ME» نوشته بود** و بابک فرستاد. تقسیم به سه بند
و خطِ رُز روی بندِ میانی از من است: بندِ میانی ادعای مرکزیِ اوست («رشد از
پذیرفتنِ مسئولیت شروع می‌شود») و باید از معرفی و از فهرستِ کارها جدا دیده
شود.

### ۳. «انتخاب» کارتِ پنجمِ مسیر شد

دفعه‌ی قبل حرفِ «لحظه‌ی انتخاب» را در یک بندِ ساده زیرِ مسیر گذاشتم. بابک
گفت منظورش این نبوده: **یک کارتِ دیگر روی خودِ همان مسیر** می‌خواست. حق با
او بود — انتخاب یکی از ایستگاه‌های داستان است، نه پانوشتِ آن.

حالا `PathCard` به‌جای `accent: boolean` یک `tone: "fall" | "choice"`
دارد و رنگِ خط هم روایت می‌کند:

| قطعه | رنگ | چرا |
|---|---|---|
| ۰۲ → ۱۶ → ۲۴ | خنثی `rgba(230,225,220,0.26)` | مسیرِ عادی |
| ۲۴ → سقوط | رُزِ تیره‌ی محو `rgba(158,31,51,0.55)` | مسیر محو می‌شود |
| سقوط → انتخاب | رُزِ روشنِ پررنگ `rgba(224,107,125,0.75)` | مسیر برمی‌گردد |

قابِ «سقوط» رُزِ تیره است و قابِ «انتخاب» رُزِ روشن. هندسه: `TOPS` به
`[0, 400, 800, 1200, 1600]` و `HEIGHT` به ۱۸۸۰ رفت. اندازه‌گیری‌شده: آخرین
کارت در ۱۷۸۷ تمام می‌شود، پس داخلِ ظرف جا می‌شود.

### ۴ و ۵. سه چیز که پاک شد

- بندِ پایانیِ «مسیر» («بازاریابی شبکه‌ای یکی از ایستگاه‌های این مسیر
  است… تعریف‌کننده‌ی کلِ مسیر نیست»). خودِ چیدمان همین را می‌گفت.
- فهرستِ «موضوع‌ها» در «صحنه» (هفت موضوع با ترجمه‌ی انگلیسی).
- دعوتِ «سارا را برای سخنرانی دعوت کنید». دعوت یک‌بار در «همکاری» می‌آید
  و همان کافی است.

«صحنه» حالا فقط شاهد است: عکس‌های واقعیِ صحنه و نوارِ نام‌های رویدادها.

### ۶. درزِ هیرو و بخشِ بعدی

بابک گفت مرزِ این دو «خشک» است و خطِ صافش دیده می‌شود. **دو تکه لازم بود،
نه یکی:**

1. `hero-v3-video.tsx` — لایه‌ای که کفِ ویدیو را در رنگِ خودِ بخش
   (`#4d1214`) حل می‌کند، پس لبه‌ی پایینیِ هیرو دیگر یک فریمِ ویدیو نیست
   بلکه یک رنگِ تخت است. `h-[16%] lg:h-[20%]`.
2. `brand-statement.tsx` — نوارِ گرادیانی در بالای بخش که از **همان**
   رنگ شروع می‌شود و به کرِم می‌رسد.

چون هر دو سر روی `#4d1214` می‌نشینند، ریاضیاً هیچ درزی نمی‌مانَد.

**نکته‌ای که فقط با چشم پیدا شد:** نسخه‌ی اولِ نوار ۹۹ پیکسل بود و با
اینکه درزی نداشت، چشم یک «نوار» می‌دید — شیب آن‌قدر کوتاه بود که گذار
ناگهانی می‌نمود. حالا `clamp(96px,19vh,230px)` است و `padding-top` بخش هم
با آن به `clamp(130px,26vh,320px)` رفت. ارتفاعِ نوار عمداً از padding
کوچک‌تر مانده، وگرنه تیتر روی قسمتِ تیره‌ی گرادیان می‌افتاد (در ۱۲۸۰:
نوار تا ۱۷۱، تیتر از ۲۳۴ — ۶۳ پیکسل فاصله).

**لایه‌ی هیرو عمداً بعد از پرده می‌آید و نه قبلش.** در موبایل پرده تا
نزدیکِ کدر تیره می‌شود و اگر این زیرش بود دیده نمی‌شد. روی پرده، کفِ کادر
`#4d1214` می‌شود و متن روی آن **بهتر** می‌شود نه بدتر: نام ۱۲.۵۴:۱، خطِ
نقش ۹.۳۷:۱، جمله ۱۲.۵۴:۱.

### سنجش

تضاد در `statement`، `about`، `story`، `journey` و `speaking`: **صفر
شکست**. سرریزِ افقی در ۳۷۵ و ۱۲۸۰ صفر. `tsc --noEmit` و `next build`
تمیز.

**دامِ پنل، دوباره:** در `zoom: 0.5` پنل بخشِ `bg-bone` را تیره رندر کرد و
یک لحظه فکر کردم گرادیان کلِ بخش را سیاه کرده. با `getComputedStyle`
معلوم شد زمینه همان `rgb(228,221,211)` و متن `rgb(31,26,22)` است؛ در
`zoom: 1` درست رسم شد. اسکرین‌شاتِ زوم‌خورده‌ی این پنل قابلِ استناد نیست.

## تایپوگرافی — پیدا

کل سایت با **Peyda v3، نسخه‌ی Standard** ست می‌شود. سه وزن، هرکدام نگاشت‌شده
به آنچه کد استفاده می‌کند: ۴۰۰ متن، ۷۰۰ `font-bold`، ۸۰۰ `font-display`.

`--font-sans` و `--font-display` هر دو همان خانواده‌اند، پس **تیترها باید وزن
را صریح بگیرند** (`font-extrabold`) — قبلاً `font-display` خانواده‌ی جدایی بود
و وزن خودش را می‌آورد.

چرا نسخه‌ی Standard:
- **Non-English** گلیف لاتین ندارد و «Personal Brand» و برچسب‌های مونو را
  نمی‌تواند ست کند (بررسی شد: ۴۱۵ گلیف، بدون `A` و رقم لاتین).
- **FaNum** رقم لاتین را به گلیف فارسی نگاشت می‌کند؛ ما ارقام فارسی را
  مستقیم یونیکد می‌نویسیم، پس Standard قابل‌پیش‌بینی‌تر است.

**پیدا نیم‌فاصله (U+200C) دارد** — با fontTools تأیید شد (۶۵۲ گلیف، همه‌ی
نویسه‌های آزمون موجود، `fsType=8` یعنی جاسازی وب مجاز). این یعنی دام
YekanBoom تمام شد و دیگر لازم نیست تیترهای نیم‌فاصله‌دار با فونت دیگری ست
شوند. یکان و YekanBoom از `public/fonts/` حذف شدند.

## گالری، قاب صفحه، و ویدیوی فوتر

### گالری کشسان
`gallery-animation.tsx` — جای کره‌ی سه‌بعدی. با `motion/react` نوشته شده، نه
`framer-motion`: پرامپت مرجع دومی را می‌خواست ولی `motion` از قبل نصب است و
همان API را دارد؛ نصب هر دو یعنی دو کتابخانه‌ی انیمیشن موازی در باندل.

**`focus` هر تصویر با موقعیت سوژه یکی نیست.** این نکته یک‌بار مرا سر کار
گذاشت: `object-position: X%` نقطه‌ی X٪ *تصویر* را با نقطه‌ی X٪ *کادر*
هم‌تراز می‌کند. چون پنجره‌ی دید در قابِ باریک حدود نصف عرض تصویر است،
گذاشتن مستقیمِ درصدِ سوژه او را به لبه می‌راند. رابطه‌ی درست:

    X = 100 × (S·nw − tw/2) / (nw − tw)

که S موقعیت سوژه (کسری از عرض)، nw عرضِ مقیاس‌خورده‌ی تصویر و tw عرض قاب.
اعداد فعلی برای قابِ **۱۰۸×۳۸۰** حساب شده‌اند (۹ قاب در `max-w-[1160px]`
منهای padding) و بعد چشمی تأیید شده‌اند — سرِ اول با ۱۲۲ حساب کردم که غلط
بود چون padding را نشمرده بودم.

بی این تصحیح، در قابِ باریک از یک عکس افقی فقط ۲۰٪ نوار میانی دیده می‌شود
و چون سارا در بیش‌تر این عکس‌ها وسط کادر نیست، آن نوار روی بنر یا صندلی
خالی می‌افتد. **اگر تعداد عکس‌ها را عوض کردی، عرض قاب و در نتیجه همه‌ی این
اعداد عوض می‌شوند.**

در موبایل ردیف کشسان به شبکه‌ی دو‌ستونی تبدیل می‌شود: در عرض کم هر قاب
حدود ۴۰px می‌شد و hover هم روی لمس معنا ندارد.

ست فعلی: ۹ عکس `g-*.webp` (۱.۱MB مجموع)، همه از `ax sara`، انتخاب خودِ
بابک. ست قبلی (`stage-*`, `pmm-*`, `roundtable-*`, `portrait-lobby-*`)
حذف شد.

`g-mastermind-turkey.webp` نفر دوم قابل تشخیص دارد. بابک صریح خواست در
گالری باشد، پس گذاشته شد؛ `focus: 16` او را از قابِ باریک بیرون می‌گذارد
ولی در نمای کامل دیده می‌شود. اگر رضایت گرفته نشده، همین یک سطر را بردار.

### قاب درخشان صفحه
`page-shine-frame.tsx` — همان مکانیک ShineBorder (گرادیان رادیالِ متحرک با
`mask-composite` که فقط نوار لبه از آن می‌ماند) ولی `fixed` روی viewport، نه
دربرگیرنده‌ی محتوا: با اسکرول بیرون نمی‌رود و چون از جریان چیدمان بیرون
است روی طول و عرض محتوا اثری ندارد. `z-40` است تا زیر مودال‌های `z-50`
بماند، و `pointer-events-none` تا کلیک نگیرد.

### ویدیوی فوتر
`footer-loop.{mp4,webm}` با شفافیت ۵۰٪ در `contact.tsx`. منبع دانلودی
۹.۳MB بود؛ به ۱۶۰۰px و بدون صدا دوباره کدگذاری شد → **۴۲۲KB / ۳۲۰KB**.

`video-backdrop.tsx` بیرون از کادر ویدیو را متوقف می‌کند، ولی حالت پیش‌فرضش
عمداً «پخش» است و توقف فقط با سیگنال واقعیِ منفی رخ می‌دهد؛ به‌علاوه تا وقتی
متوقف است هر ۸۰۰ms هندسه را خودش می‌سنجد. دلیلش را در دام شماره‌ی ۱۱ بخوان.

## دو کامپوننت از 21st.dev

MCP سایت 21st نصب شد (`claude mcp add --transport http 21st …`، کلید در
`~/.claude.json` — **بابک باید این کلید را عوض کند**، چون در چت رد و بدل
شد). دو کامپوننت برداشته شد و هیچ‌کدام دست‌نخورده استفاده نشد.

**آنچه برداشته نشد:** `count-up`. برای این سایت نمی‌خورَد: عددهای سارا
فارسی‌اند («۱۵٬۰۰۰») و یکی‌شان «چندصد» است که اصلاً عدد نیست؛ شمارنده
خوانایی را به جاوااسکریپت گره می‌زند.

### ۱. نوارِ حضور — `ui/logo-marquee.tsx`

از `ddoemonn/logo-marquee`. منطقِ حرکتش (rAF، شتاب‌گیریِ نمایی، ایستادن با
hover و focus) دست‌نخورده ماند چون درست نوشته شده و — مهم‌تر — در
`prefers-reduced-motion` به یک فهرستِ اسکرول‌شدنی تبدیل می‌شود نه اینکه
محتوا را پنهان کند. هیچ وابستگیِ npm تازه‌ای نیاورد.

در `sections/speaking.tsx` زیرِ گالری سوار شد، با همان پنج موردی که در
بازطراحیِ بدنه گم شده بودند: PMM First Leadership ۲۰۲۳ · PMM 13th
Anniversary · Mastermind Event · دانشگاه رضوی مشهد · همایش تونی رابینز.
هر پنج مورد پشتوانه‌ی عکس دارد؛ ادعای تازه‌ای اضافه نشد.

**دامِ RTL — دو بار پشتِ سر هم خوردم:**

1. ریاضیِ این کامپوننت فرض می‌کند سرریزِ نوار به **چپِ** مبدأ می‌رود، که
   فقط در LTR درست است. در ظرفِ راست‌به‌چپ، `translate3d(-1052px)` که
   حالتِ سکونِ آن است کلِ نوار را از کادر بیرون می‌انداخت: هندسه سالم بود،
   نوار خالی دیده می‌شد. راه‌حل: ظرفِ داخلی `dir="ltr"` تا همان ریاضیِ
   آزموده کار کند، و هر برچسب `dir="auto"` تا جهتِ خودش را از متنِ خودش
   بگیرد (فهرست هم فارسی دارد هم لاتین).
2. جهتِ حرکت را اول `right` گذاشتم. غلط بود: چشمِ فارسی‌خوان از راست شروع
   می‌کند، پس نامِ تازه باید از لبه‌ی **راست** وارد شود، یعنی نوار به چپ
   برود. `direction="left"` به‌علاوه‌ی `flex-row-reverse` روی هر گروه، تا
   اسکنِ راست‌به‌چپ ترتیبِ ۱،۲،۳،۴،۵ بدهد. سنجیده شد.

آیتم‌ها از لینک به متنِ ساده تغییر کردند: این رویدادها آدرسی ندارند که به
آن لینک شود، و لینکِ بی‌مقصد فقط یک ایستگاهِ خالیِ Tab می‌سازد.

### ۲. ریلِ روش — `ui/method-rail.tsx`

از `manuarora700/timeline` (Aceternity). فقط ایده‌اش برداشته شد — خطِ
کم‌رنگ در تمام ارتفاع و پرتویی که هم‌قدمِ اسکرول پر می‌شود — و سه چیزش
عوض شد: گرادیانِ بنفش-آبیِ پیش‌فرض (همان ظاهری که از سایت پاک کردیم) به
رُز، `left-8` به `start-*` برای RTL، و اندازه‌گیریِ یک‌باره‌ی ارتفاع به
`ResizeObserver` — وگرنه بعد از لودِ فونتِ پیدا پرتو کوتاه‌تر از ریل
می‌مانْد.

`sections/sara-method.tsx` از پنج کارتِ جدا به یک ریلِ پیوسته تغییر کرد.
دلیلش معنایی است نه تزئینی: کارت‌های جدا پنج چیزِ مستقل نشان می‌دادند و
فلش‌های بینشان تنها چیزی بودند که می‌گفتند ترتیبی در کار است؛ ادعای این
بخش همان ترتیب است، پس ریل خودش آن را می‌گوید. شماره‌ها ماندند.

**سنجش:** نقطه‌ها و خطِ ریل هر دو دقیقاً ۷ پیکسل از لبه‌ی راست‌اند (هم‌راستا
در RTL، در ۳۷۵ و ۱۲۸۰). سرریز افقی صفر. تضاد با خواندنِ پیکسل از canvas:
شماره ۳.۶۶:۱ در ۲۰px/۸۰۰ (آستانه‌ی متنِ درشت ۳:۱)، عنوان ۱۳.۷، متن ۶.۷۴،
نوارِ حضور ۶.۷۴ در ۱۳px. `tsc --noEmit` و `next build` هر دو تمیز.

**آنچه سنجیده نشد:** خودِ حرکت. پنل مرورگر در این نشست اصلاً اسکرول نمی‌کرد
(`scrollHeight` ۱۳۹۷۱ و `scrollTop` همیشه ۰، چه با `scrollTo` چه با چرخِ
واقعی) و rAF هم پیش نمی‌رفت. پس رشدِ پرتو و چرخشِ نوار روی مرورگرِ واقعی
باید دیده شود. حالتِ سکونِ هر دو درست است: گام‌ها و نام‌ها بدون هیچ
انیمیشنی کامل دیده می‌شوند.

**نکته‌ی سنجش:** خواندنِ رنگ با `ctx.fillStyle` برای رنگ‌های Tailwind v4
غلط جواب می‌دهد — canvas مقدارِ `oklab(... / 0.75)` را همان‌طور برمی‌گرداند
و پارسِ عددی آن را ۰ تا ۱ می‌خواند، نه ۰ تا ۲۵۵. یک‌بار همین باعث شد
شماره‌ی گام‌ها ۱.۱۴:۱ سنجیده شود در حالی که واقعاً ۳.۶۶:۱ است. رنگ را باید
روی canvas کشید و **پیکسل** را خواند.

## دام‌هایی که قبلاً خوردیم — تکرار نکن

1. **`overflow-x` روی `<html>` نگذار.** `clip` یا `hidden` روی ریشه، اسکرول سند را می‌کشد (`scrollHeight === 0`) و `useScroll` و IntersectionObserver را از کار می‌اندازد. فقط روی `body` با `clip`.
2. **RTL: محورهای منطقی با فیزیکی قاطی نشوند.** `justify-end` در RTL یعنی سمت **چپِ** فیزیکی. برای راستِ فیزیکی از `justify-start` استفاده کن. `clip-path` و `left/right` همیشه فیزیکی‌اند.
3. **ارتفاع درصدی روی والدِ `min-h-svh` صفر می‌شود** (ارتفاع والد `auto` است). برای پارالاکس از `absolute inset-0` + `scale` استفاده کن، نه `h-[112%]`.
4. **Tailwind v4 نحو کوتاه `[prop:--var]` را حذف کرده.** حتماً `var()` صریح بنویس (`before:p-[var(--border-width)]`). خطا نمی‌دهد، فقط بی‌صدا کار نمی‌کند — همین ShineBorder را شکسته بود.
5. **`@keyframes` را داخل `@theme` نگذار** — tree-shake می‌شود.
6. Next 16: `priority` منسوخ شده → `preload`. `quality` غیر از ۷۵ باید در `next.config.ts` داخل `images.qualities` اعلام شود وگرنه بی‌صدا به ۷۵ برمی‌گردد.
7. **`F_yekan.ttf` غیریونیکد است** (گلیف فارسی روی کدپوینت لاتین، `fsType=1`). استفاده نکن. فقط woff2‌های داخل `public/fonts/`.
8. **ارتفاع تصویر تمام‌کادر را به کل بخش نبند.** در موبایل ستون‌ها روی هم می‌چینند، بخش بلند می‌شود، و `absolute inset-0` کادر عکس را عمودی می‌کند؛ آن‌وقت `object-cover` تصویر افقی را تا ۴ برابر بزرگ می‌کند و از پرتره فقط یک تکه مو می‌ماند. راه‌حل: در موبایل نوار `h-[58svh]` بده و در `lg` به `inset-0` برگردان.
9. **ناظرها را تنها منبع اندازه‌گیری نکن.** `ResizeObserver` هم مثل `IntersectionObserver` در پنل مرورگر ممکن است هیچ callback ندهد؛ آن‌وقت افکت بی‌صدا از کار می‌افتد. هر افکتی که به ابعاد وابسته است باید `measure()` فوری و شنونده‌ی `resize` هم داشته باشد و ناظر فقط پشتیبان باشد.
10. **پنل مرورگر گاهی تصویر و ویدیو را کامپوزیت نمی‌کند** در حالی که CSS را رسم می‌کند. اگر هندسه با `getBoundingClientRect` درست بود ولی اسکرین‌شات خالی آمد، ایراد در کد نیست. با یک تب تازه امتحان کن یا ترکیب را سمت سرور با PIL رندر کن.
11. **ناظر ممکن است یک‌بار کار کند و بعد ساکت بماند.** در پنل مرورگر دیدم
    که `IntersectionObserver` در mount یک callback می‌دهد و بعد هیچ گذارِ
    ورود به کادر را خبر نمی‌دهد؛ `scroll` و `resize` هم اصلاً نمی‌رسند
    (`scrollEventsFired: 0` در حالی که `scrollY` عوض شده بود). هر افکتی که
    «بیرون از کادر خاموش شود» باید حالت پیش‌فرضش **روشن** باشد و فقط با
    سیگنال منفی خاموش شود، وگرنه در چنین محیطی هرگز روشن نمی‌شود و خرابی
    بی‌صدا می‌ماند. یک بازبینی کم‌فرکانس (۸۰۰ms) هم فقط در حالت خاموش
    نگه‌دار، تا از گیرکردن دربیاید.
12. **`AnimatePresence` می‌تواند یک مانع کلیکِ نامرئی جا بگذارد.** تا انیمیشن
    خروج تمام نشود عنصر mount می‌ماند؛ اگر آن عنصر `fixed inset-0` باشد، با
    `opacity: 0` نامرئی است ولی همچنان کلیک می‌گیرد و کل صفحه قفل می‌شود.
    اگر rAF معلق شود (تبِ پس‌زمینه، بعضی webview‌ها) انیمیشن هرگز تمام
    نمی‌شود. گذاشتن `pointerEvents` داخل `exit` جواب نمی‌دهد چون آن هم از
    همان خط لوله می‌گذرد — همان لحظه‌ی بستن مستقیم روی DOM خاموشش کن.
13. **کامپوننتِ آماده‌ی افقی را در RTL باور نکن.** هر مارکی/کروسل که با
    `translateX` کار می‌کند فرضِ نانوشته‌ای دارد: سرریز به چپِ مبدأ می‌رود.
    در `direction: rtl` مبدأ لبه‌ی راست است و همان کد نوار را از کادر بیرون
    می‌اندازد — بی خطا، فقط خالی. یا ظرفِ محاسبات را `dir="ltr"` کن و
    جهتِ خواندن را با `flex-row-reverse` برگردان، یا کلِ علامت‌ها را
    بازنویسی کن. نصفه‌کاره نکن.
14. **YekanBoom نویسه‌ی ZWNJ (U+200C) ندارد** — کلمات نیم‌فاصله‌دار را با یکانِ معمولی بنویس.

## دارایی‌ها

- `public/fonts/`: `Peyda-{Regular,Bold,ExtraBold}.woff2` (۱۳۲KB) — از `Fonts/Peyda-v3.zip`، نسخه‌ی **Standard**
- `public/images/`: ۶۳ فایل؛ کلیدی‌ها `hero-oil-2048.jpg` (نقاشی هیرو)، `hero-bw-2048.jpg`، مجموعه‌ی گالری
- `public/video/`: `hero-loop.mp4`, `hero-loop.webm`, `hero-loop-poster.jpg`
- منبع اصلی عکس‌ها: `C:\Users\BABAK\OneDrive\Desktop\ax sara`

## کارهای باز

- [ ] داوری بصری هیرو توسط بابک (من نمی‌توانم زیبایی را قضاوت کنم)
- [ ] لینک‌های واقعی اینستاگرام/لینکدین (الان `#`)
- [ ] ایمیل واقعی (الان `hello@saradavoudifar.com` جای‌نگهدار)
- [ ] سه نمونه‌ی بی‌نام نتایج سارا برای بخش «اثبات»
- [ ] فایل‌های اصلی و باکیفیت عکاس
- [ ] بودجه و زمان‌بندی پروژه
- [ ] `_lobby-with-guest` تا گرفتن رضایت نفر دوم کنار گذاشته شده
- [ ] Firecrawl MCP — بابک باید خودش `claude mcp add` را بزند (کلسیفایر امنیتی جلوی من را می‌گیرد)
- [ ] استقرار (deploy) هنوز شروع نشده

## قواعد کار که بابک تعیین کرده

- **از سایت‌های مرجع کپی نکن.** فقط اصل تعامل، نه ظاهر.
- **هیچ چهره‌ای با AI ساخته یا مورف نشود.** فقط عکس‌های واقعی سارا.
- تم سینماتیک، هدر و فوتر شیک، فونت فارسی یکان.
